# Onboarding v2

## Purpose

A refined, continuous connection experience for the Proton VPN Windows app. Evolves v1 into a single cinematic flow with four states that animate seamlessly into one another. The user sees their real exposed data, protects themselves with one click, watches the VPN take effect in real time, then picks what matters most (JTBD) and gets country-specific context.

## Status

In Progress — all four states implemented and wired as a continuous flow. Reachable from the start screen via the "Onboarding v2" button.

## Stages

The end-to-end flow is sliced into four named stages (`OnboardingStage` / `ONBOARDING_STAGES` in `OnboardingV2.tsx`). The first three are phases inside the `OnboardingV2` state machine; the fourth is a separate modal shown once in the main app.

| Stage | Name | Phases | Lives in |
|-------|------|--------|----------|
| 1 | **Establishing VPN connection** | `unprotected` → `connecting` → `protected` | `OnboardingV2.tsx` |
| 2 | **Personalized JTBD tuning** | `jtbd` → `tuned` | `OnboardingV2.tsx` |
| 3 | **Upgrade to Plus** | `upsell` → `web-checkout` → `checkout` → `plus-welcome` | `OnboardingV2.tsx` |
| 4 | **Final personalization** | — (`MakeYoursModal`, not a phase) | `App.tsx`, `components/MakeYoursModal.tsx` — see "Final personalization modal" below |

`PHASE_STAGE` maps each phase to its stage and is used internally (e.g. `dim`/`isProtectedSide` on the map) instead of long per-phase OR-chains. Render sections in `OnboardingV2.tsx` are marked with `Stage N:` banner comments matching this table.

## States / Key User Flows

`Unprotected → Connecting → Protected → JTBD Selection → Tuning → Tuned Result → VPN Plus Upsell → [Simulated Web Checkout → Checkout Loader → VPN Plus Welcome] → App transition → "Make Proton VPN yours" modal`

1. **Unprotected** — Dark map centered on the user's real location. Coral open-padlock header, danger headline, ISP warning. Red pulsating pin framed by an animated sci-fi targeting reticle (`TargetingReticle`). Glassmorphic info card (3 rows: country+flag, IP, ISP). CTA: "Protect my online identity".
2. **Connecting** — Spinner header, "Protecting your online activity....". Background gradient morphs coral → slate. Reticle fades out, pin turns gray, map slowly zooms out. Card values progressively redact to asterisks (labels stay readable). VPN connect is simulated (min 3.2s).
3. **Protected** — Teal closed-padlock header, "...now fully protected!". Gradient morphs slate → teal. Map flies to the VPN server (Netherlands). Teal pulsating pin wrapped in an animated protective shield aura (`ShieldAura`). Info card rebuilds to 4 staggered rows (location hidden, appears-in country+flag, IP hidden, VPN IP in teal). CTA: "Continue".
4. **JTBD Selection** (`JtbdGridPanel`) — a centered 3×2 grid of 6 selectable JTBD tiles (icon + label), a small contextual "wink" line + idea-bubble illustration once a tile is picked, and a dynamic Continue ("Continue" → "Tune for {jtbd}" once selected) + top-right Skip. Picking a tile again deselects it. Continue advances straight to `tuned` (no separate loader — see below); Skip exits.
5–6. **Tuned Result** (`TunedResult`, phase `tuned`) — a single, consolidated result step with no separate loader: opens with its own centered intro (see "Header intro/move/counter" below) that becomes the result header, then the 5 settings materialize one at a time into the selected **layout** (Stacked / Split by Status / Card Grid / Compact List — see "Tuning-stage layouts" below), then completes with a "Tuned for {jtbd}" title, a derived summary subtext, and Continue. **No tip** anywhere on this screen (removed across all 4 layouts). Top-left Back button returns to JTBD selection. Plus users (`userPlan="plus"`) see all 5 items as applied (no locked items, no tier-boundary widget).
7. **VPN Plus Upsell** — Split-screen conversion screen. "Get VPN Plus" → simulated web checkout (below); "Continue free" → exit.
7.5. **Simulated Web Checkout** (`web-checkout`, `SimulatedWebCheckout`) — a fake Chrome browser window opens on top of the (still-mounted, dimmed, inert) upsell screen, containing a prototype "Review subscription and pay" page. See "Simulated web checkout" below for the full breakdown. Happy path only — no cancel/close/abandon handling; window controls are decorative. Ends with the user clicking the dimmed app behind (only live post-payment-success), which advances to the Checkout Loader.
8. **Checkout Loader** — Transparent overlay reusing `LoaderScreen` (same teal gradient background), UNCHANGED. Title "Completing checkout...", subtitle "Confirming your payment. This takes a few seconds." 3000ms fixed timer (no real API), then advances to Plus Welcome.
9. **VPN Plus Welcome** (`PlusWelcomeState`) — Renders the same 5-item tuned result in whichever of the 4 **result layouts** (Stacked / Split by Status / Card Grid / Compact List) the shared "Layout" selector currently has active — see "Stage 3 reuses stage 2's layouts" below. All 3 free items render active from the start; the 2 previously-locked paid items transform locked → unlocked at ~600ms settle (badge crossfades to green check, text brightens, "Available" value pill slides in where that layout's own resolved design shows one, "Just unlocked" chip), positioned per layout (last 2 rows / right column / bottom 2 cards). Both transform with a 200ms stagger. Confetti fires once. **No tip** (removed, matching the Tuned Result step). Continue ("Start using VPN Plus") fires `onEnterApp` (hands off to the main app — assumes VPN is already connected to Netherlands from earlier in the flow).

**The upsell screen** (`VPNPlusUpsell`). Left (~52%): "What VPN Plus unlocks for you" heading, JTBD-personalized subheading, 3 JTBD-specific benefit cards (green check + outcome + "via {feature}" + (i) tooltip), an italic "everything else" line, a factual trust-signal row, and two CTAs. Pricing is folded into the primary CTA: "Get VPN Plus" with a concise second line ("$4.99/mo, billed yearly · save 50%"); the secondary ghost "Continue free" stays clearly available. Right (~48%): black panel with the 3D ▽+ hero image bleeding to the edges. A top-left Back button returns to the Tuned Result. Content density is tuned to fit the 768px window without scrolling. Uses Tailwind container queries (`@container`): below ~900px panel width the hero is hidden and content goes single-column. "Continue free" still exits directly; **"Get VPN Plus" now opens the simulated web checkout** (`web-checkout` phase) instead of going straight to the checkout loader. Lucide icons (Info, ArrowUpRight, ShieldCheck, BadgeCheck, RefreshCw); Radix Tooltip on (i) icons.

### Simulated web checkout (`web-checkout` phase)

Inserted between the upsell CTA and the existing checkout loader, simulating "leaving the app to pay on the web" without a real payment backend (happy path only — no cancel/close/abandon handling; see Forbidden Actions in the originating task).

**Why built fresh, not embedded from a reference source:** a real Proton checkout page was inspected as a potential reuse source, but it turned out to contain only the Chargebee/Stripe/hCaptcha payment-processor iframe plumbing (hosted card-number/expiry/CVC iframes, a bot-challenge widget, a cross-origin fraud-detection bridge) — no visual "Review subscription and pay" page (no plan cards, no Summary panel, no totals/tax math) exists in that source to reuse, and the iframes that are there only function against a live backend. `CheckoutPage.tsx` is therefore a new prototype component, matching a provided screenshot's copy/layout/numbers exactly rather than reusing that source.

- **`SimulatedWebCheckout.tsx`** (`components/checkout/`) — orchestrator. Renders the upsell screen again as the dimmed, `aria-hidden`, fully inert "app behind" (never unmounted, per Scope Lock), a full-bleed overlay `<button>` that doubles as the dimming layer AND the "click to return" target (disabled — cursor-default, darker dim — until payment succeeds; live — cursor-pointer, lighter dim, plus a fading-in "Click on the VPN app window to continue" hint — once it does), and the browser window on top. Its root is an isolated stacking context: the backdrop wrapper is explicitly `z-0` (confining `VPNPlusUpsell`'s own internal `z-[1000]`), the dim/return layer is `z-10`, browser `z-20`, and post-success hint `z-30`. This explicit layering prevents the reused upsell from painting over and hiding the fake browser. Owns `processing`/`success`/`returning` state and the two timers (pay→success, return-click→exit) — both centralized in `checkoutTiming.ts`.
- **`BrowserWindowChrome.tsx`** — the Chrome-style shell: one-tab tab bar (Proton VPN favicon from `assets/proton-vpn-logo.png` + "Proton VPN — Review subscription"), address bar (padlock + simulated `account.proton.me/vpn/checkout` URL), decorative minimize/maximize/close controls. All browser-view copy uses **Inter** (loaded via `fonts.css`). Sized to 88%×88% via `inset-[6%]`, so the dimmed app is visible around every edge. This inset-based positioning deliberately avoids translate-based centering: Framer Motion owns the element's `transform` for its scale animation, so combining scale with Tailwind `translate(-50%)` could overwrite the centering transform. Scale 0.96→1 + fade entrance/exit (~250ms each way); `prefers-reduced-motion` (`useReducedMotion`) drops the scale for a plain fade.
- **`CheckoutPage.tsx`** — the embedded "Review subscription and pay" content: 3 selectable subscription-option cards (24/12/1-month, `lib/checkoutPlans.ts`), 4 selectable payment methods (only Credit/debit card reveals card fields; no validation on any field), a Summary panel (plan badge/term, per-month rate, total, credits, amount due, "Pay US$X now", tax note, expandable "What do I get?"). Selecting a plan re-renders the whole Summary from `checkoutPlans.ts`.
- **`CheckoutSuccess.tsx`** — replaces the checkout content once "Pay" resolves: check mark, "Payment successful", "You're all set — return to the Proton VPN app to continue."
- **`lib/checkoutPlans.ts`** — the 3 plans + derivation math, entirely from screenshot facts (24-month: total US$119.76, -50%/"Save US$120", credits -US$1, amount due US$118.76, "Including 21% tax: US$20.78", renews after the term then billed every 12 months at US$83.88; 12-month: US$6.99/mo, "For US$83.88, Save US$36"; 1-month: US$9.99/mo). Everything else is DERIVED, not invented: amount due = total − US$1 credits; tax = total × 0.21/1.21 (VAT-inclusive, verified against the 24-month screenshot figure); the "Save" badge/amount is measured against the US$9.99 anchor monthly rate for the same term. Renewal dates are computed from the real current date + the plan's term (not hardcoded), so they stay correct as time passes.
- **`lib/checkoutCopy.ts`** — every browser-chrome/page/summary/success string, centralized (no i18n framework exists in this codebase — see "Tone of voice" below for the established precedent this follows: strings live in one exported object, not inlined in JSX).
- **`checkoutTiming.ts`** — `CHECKOUT_TIMING`: window entrance/exit (250ms each), page-load spinner (`pageLoadDuration`, 2000ms), pay→processing→success (2000ms), success fade-in (300ms).

**Page load:** the moment the browser window mounts, it shows a plain spinner (a small CSS `animate-spin` ring, not the shared white-stroke `Spinner` component — that one's designed for the dark app background, not this page's light `#f4f5f9` one) for `pageLoadDuration` (2s) before the "Review subscription and pay" content appears — simulating the page actually loading, the same beat a real browser navigation would have. Runs regardless of `prefers-reduced-motion` (`SimulatedWebCheckout.tsx`'s own `pageLoading` state/timer) — this is a simulated page-load delay, not a decorative animation choice.

**Return flow:** clicking "Pay" shows a spinner (reusing `Spinner`) for `payProcessingDuration`, then swaps to `CheckoutSuccess`. Only then does the dimming overlay become clickable; clicking it animates the browser window out (scale/fade) and, after `windowExitDuration`, calls `onReturnToApp` → `OnboardingV2` sets `phase` to the EXISTING `checkout` phase (`LoaderScreen`, unchanged) → `plus-welcome` (unchanged). Pre-success clicks on the dimmed app do nothing (the overlay button is `disabled`).

## Data Model

- **`GeoInfo`** (`lib/useIpDetection.ts`) — live IP detection via `ipwho.is` with a Figma-matching fallback (Lithuania / Orange / 158.6.140.191). Fields: `ip, country, countryCode, city, isp, ispKnown, lat, lng`. `ispKnown` gates connection-stage copy: when true, subtext names the provider as a lead example (`"BT"`) plus "others along the way/in between"; when false (or when `isp === ISP_GENERIC_LABEL`, `"your internet provider"`), copy uses broad phrasing without a concrete name. The real country is captured before connecting and reused for the JTBD panel (the VPN is active by then and would otherwise report the server country).
- **`VPN_SERVER`** (`lib/server.ts`) — fixed "fastest" server (Netherlands / Amsterdam / `146.70.124.18`).
- **JTBD context matrix** (`lib/jtbdData.ts`) — 6 jobs × {UK, US, Germany, France, Netherlands, fallback} factual copy.
- **JTBD wink copy** (`JTBD_WINK_COPY` in `lib/jtbdData.ts`) — one short, knowing line per JTBD, shown by `JtbdGridPanel` next to the idea-bubble illustration once a card is picked. With the user, never at them; no capability-overclaiming; no emoji.
- **JTBD Continue-label copy** (`JTBD_CONTINUE_LABEL` + `JTBD_CONTINUE_LABEL_DEFAULT` in `lib/jtbdData.ts`) — the grid picker's primary CTA text: `JTBD_CONTINUE_LABEL_DEFAULT` ("Continue") pre-selection, one full "Tune for {jtbd}" string per JTBD once picked (not runtime-concatenated, so each locale can adapt word order / the "access" mapping for "Bypass restrictions" naturally).
- **JTBD tuning result** (`lib/jtbdTuningResult.ts`) — per-`JtbdId` tuning outcome (`JTBD_TUNING_RESULT`): `jtbdLabel`, exactly 3 `enabled` features (`outcome`, `settingsName`, `value`, `tooltip`), exactly 2 `paid` features (`outcome`, `featureName`, `asset`, `tooltip`), and a `tip` (string | null). Single source of truth for the Tuning/Tuned screens — no copy is hardcoded in components. Every feature has a short hover `tooltip`; `privacy.tip` is `null` pending confirmation.
- **JTBD upsell** (`lib/jtbdUpsell.ts`) — per-`JtbdId` upsell content (`JTBD_UPSELL`): `jtbdWord` (subheading injection) + exactly 3 `benefits` (`outcome`, `featureName`, `learnMore`, `tooltip`). Plus `UPSELL_PRICING`, `UPSELL_EVERYTHING_ELSE`, `UPSELL_TRUST_SIGNALS`. Single source of truth for the upsell screen. Pricing/trust values are verified against Proton's public June-2026 pricing/policy (monthly $9.99, 30-day money-back guarantee, Swiss-based, independently audited no-logs); `[UNVERIFIED]` the yearly $4.99/mo is design-confirmed but Proton's current public 1-yr rate is $3.99/mo — confirm region/promo before shipping.
- **Checkout plans** (`lib/checkoutPlans.ts`) — the 3 simulated web-checkout plans (24/12/1-month) + derived Summary math (amount due, VAT-inclusive tax, save amount/percent, renewal date/sentence). See "Simulated web checkout" above for the full derivation.
- **Checkout copy** (`lib/checkoutCopy.ts`) — all simulated web-checkout strings (browser chrome, page, Summary, success).
- **JTBD merge engine** (`lib/jtbdMerge.ts`) — Multiple-mode's data layer. See "Multiple-mode tuning" below for the full writeup.
- **JTBD profile labels** (`JTBD_PROFILE_LABEL` in `lib/jtbdData.ts`) — short capitalized preview label per JTBD ("Streaming", "Access", etc.), used only by the Multiple-mode profile-preview bridge.

## Components

| Component | Path | Purpose |
|-----------|------|---------|
| `OnboardingV2` | `src/app/onboarding-v2/OnboardingV2.tsx` | Orchestrator: phase state machine, choreography, header/card/CTA, AnimatePresence. Exports `ONBOARDING_STAGES` / `PHASE_STAGE` — see "Stages" above |
| `OnboardingMapV2` | `src/app/onboarding-v2/OnboardingMapV2.tsx` | Single persistent Leaflet map, pin, targeting reticle, StatusGradient; blurs behind the JTBD workspace |
| `TargetingReticle` | `src/app/onboarding-v2/components/TargetingReticle.tsx` | Multi-layer animated sci-fi HUD reticle (rotating rings, tick dial, crosshair, scan sweep, directional arrows, breathing brackets) — danger/coral, Unprotected pin |
| `ShieldAura` | `src/app/onboarding-v2/components/ShieldAura.tsx` | Multi-layer animated protective force-field aura (breathing shield arcs, counter-rotating inner arcs, moiré encryption dot-rings, blurred breathing halo, drifting particles, outward chevrons, organic shimmer) — protected/teal, Protected pin |
| `JtbdGridPanel` | `src/app/onboarding-v2/JtbdGridPanel.tsx` | The stage's ONLY JTBD picker — a centered 3×2 grid of selectable tiles (`JtbdGridTile.tsx`); `onSelect` accepts `JtbdId \| null` — clicking an already-selected tile again deselects it. Between the grid and Continue: a small "wink" message (`JTBD_WINK_COPY` in `lib/jtbdData.ts`, one line per JTBD) next to the idea-bubble illustration reused from `TuningTip`, space always reserved so selecting/deselecting a card never shifts Continue; illustration stays put, only the text crossfades across picks, and deselecting fades the whole message out; no emoji (kept consistent with the rest of the app's copy, which never uses one). The Continue button's label is also dynamic: "Continue" by default (including once deselected), "Tune for {jtbd}" once a card is picked (`JTBD_CONTINUE_LABEL`, full per-JTBD strings — "Bypass restrictions" → "Tune for access" — never built via prefix concatenation), crossfading between labels; a fixed `min-width` (sized to the longest label) keeps the button from resizing as it changes |
| `InfoCard` | `src/app/onboarding-v2/components/InfoCard.tsx` | Glassmorphic card; `MaskedValue` (scramble), `FlagValue`; supports `heading`, `footnote`, per-row `icon` (for content variants) |
| `WindowChrome` | `src/app/onboarding-v2/components/WindowChrome.tsx` | Proton VPN title bar — logo mark is the real, transparent-background `assets/proton-vpn-logo.png` (user-provided), not a hand-drawn SVG. Minimize/maximize stay decorative; the "X" is a real interactive control (`onClose` prop, threaded from `OnboardingV2`'s own `onClose` prop) — clicking it closes the window straight back to the prototype's initial start screen (the 3-button screen), distinct from `onExit` (the normal onboarding-completion handoff to the main app). `App.tsx`'s `handleCloseOnboarding` just sets `AppState` back to `"start"`; `OnboardingV2` fully unmounts, so its internal phase/selection state resets naturally the next time onboarding starts. |
| `Spinner` | `src/app/onboarding-v2/components/Spinner.tsx` | Connecting arc spinner |
| `Padlocks` | `src/app/onboarding-v2/components/Padlocks.tsx` | Shared `PadlockOpen` (coral) / `PadlockClosed` (teal) header icons |
| `ControlPanelOverlay` | `src/app/onboarding-v2/components/ControlPanelOverlay.tsx` | v2 side-panel layout for states 1–3 (left rail dashboard + offset map) |
| `LoaderScreen` | `src/app/onboarding-v2/components/LoaderScreen.tsx` | Generic parameterized loader: `title`, `subtitleLine1`, optional `subtitleLine2`, `durationMs`, `onComplete`. Shared by the checkout step (the tuning stage no longer uses a separate loader — see `TunedResult` below). |
| `PlusWelcomeState` | `src/app/onboarding-v2/components/PlusWelcomeState.tsx` | Plus welcome (state 9): now `layout`/`tone`-aware — reuses stage 2's SAME 4 layout renderers (`tuned-result/layouts/*`), fed the "all 5 resolved, 2 paid animating locked→unlocked" state via each layout's `unlockTransition` prop. Owns its own header (Welcome title/JTBD subheading), confetti, and CTA (Continue fires `onEnterApp`); item rendering/transition is fully delegated to the reused layouts + `TransformingPaidCell`. See "Stage 3 reuses stage 2's layouts" below. |
| `TunedResult` | `src/app/onboarding-v2/tuned-result/TunedResult.tsx` | The stage's single, consolidated result step (states 5–6 collapsed into one — no separate loader). See "Tuning-stage layouts" below for the full breakdown. |
| `EnabledFeatureRow` | `src/app/onboarding-v2/components/EnabledFeatureRow.tsx` | Enabled settings row: green check (spring scale-pop) + outcome + settings-name + value pill + (i) Radix tooltip. `layout?: "row" \| "stacked" \| "card"` (default `"row"`, pixel-identical to the original) picks the container arrangement — `"stacked"` (narrower column: outcome on top, meta below, info icon top-right) and `"card"` (vertical card face: glyph top-left, info top-right, clamped outcome, meta bottom) are reused by the Split by Status / Card Grid tuned-result layouts (see below), and `"row"` by Compact List; same `feature` data and content logic in all three, never forked. Still reused unchanged by `PlusWelcomeState` too. |
| `PaidFeatureRow` | `src/app/onboarding-v2/components/PaidFeatureRow.tsx` | Paid feature row: ▽+ badge + outcome + decorative asset + feature-name + (i) Radix tooltip. `unlocked` prop renders it as enabled (green check, full color) for Plus users. Same `layout` prop (and `"stacked"`/`"card"` modes) as `EnabledFeatureRow` |
| `VPNPlusUpsell` | `src/app/onboarding-v2/components/VPNPlusUpsell.tsx` | Split-screen Plus upsell (state 7): JTBD-aware benefit cards, price anchor/savings, trust signals, two CTAs; container-query responsive; 3D hero on the right |
| `SimulatedWebCheckout` | `src/app/onboarding-v2/components/checkout/SimulatedWebCheckout.tsx` | `web-checkout` phase orchestrator: dimmed inert upsell backdrop + click-to-return overlay + `BrowserWindowChrome`. See "Simulated web checkout" above |
| `BrowserWindowChrome` | `src/app/onboarding-v2/components/checkout/BrowserWindowChrome.tsx` | Decorative Chrome-style window shell (tab bar, address bar, window controls) wrapping the checkout content; scale+fade entrance/exit |
| `CheckoutPage` | `src/app/onboarding-v2/components/checkout/CheckoutPage.tsx` | The embedded "Review subscription and pay" prototype page: plan cards, payment methods, card fields (no validation), Summary panel |
| `CheckoutSuccess` | `src/app/onboarding-v2/components/checkout/CheckoutSuccess.tsx` | Post-payment success state shown inside the browser window |

### Lib

| File | Purpose |
|------|---------|
| `lib/mapKit.ts` | Tile URL, pin HTML factory + CSS, pin status colors (adapted from v1) |
| `lib/useProgressiveMask.ts` | Progressive asterisk redaction hook (adapted from v1) |
| `lib/useIpDetection.ts` | Live IP geolocation + fallback |
| `lib/jtbdData.ts` | JTBD options + country context matrix + `JTBD_WINK_COPY` (grid picker's per-JTBD wink line) + `JTBD_CONTINUE_LABEL`/`JTBD_CONTINUE_LABEL_DEFAULT` (grid picker's dynamic Continue label) |
| `lib/server.ts` | VPN server constants |
| `lib/entranceTiming.ts` | Centralized `ENTRANCE_TIMING` config for the Unprotected entrance |
| `lib/jtbdTuningResult.ts` | `JTBD_TUNING_RESULT` data: per-JTBD enabled/paid features + tip for the Tuning/Tuned screens |
| `lib/jtbdTuningToneCopy.ts` | `JTBD_TONE_OUTCOMES` (per-JTBD outcome/tip text × 4 tones) + `toneOutcome`/`toneTip` lookup helpers — see "Tone of voice" below |
| `lib/jtbdUpsell.ts` | `JTBD_UPSELL` data + pricing/trust constants for the VPN Plus upsell screen |
| `lib/toneOfVoice.tsx` | Tone-of-voice content axis: `ToneOfVoice` type, `TONE_OPTIONS`, `STAGE_SUPPORTS_TONE` (now includes `"tuning"`), `CONNECTION_COPY` (stage 1's per-tone `mapSpotlight`/`browsing`/`hybrid` copy) and `TUNING_COPY` (stage 2's per-tone structural copy). See "Tone of voice" below |

### Assets

`src/app/onboarding-v2/assets/` — real SVGs exported from Figma node `19585-5045` (not recreations): `checkmark-circle-filled.svg` (enabled check), `info-circle.svg` ((i) tooltip trigger), `vpn-plus-badge.svg` (▽+ paid badge), `tip-illustration.svg` (Tip lightbulb). `proton-vpn-logo.png` — the real Proton VPN triangle logo mark (user-provided, transparent background), used by `WindowChrome`'s title-bar logo, replacing a hand-drawn gradient-triangle SVG approximation.

Paid-row **decorative icons**: most are dedicated 32×32 illustrations exported from Figma node `19586` and stored in `assets/` (`feature-netshield`, `feature-secure-core`, `feature-moderate-nat`, `feature-server-breadth`, `feature-vpn-accelerator`, `feature-port-forwarding`, `feature-p2p-servers`, `feature-network-warning`). The four features without a dedicated illustration fall back to the existing Proton VPN profile glyphs (`src/imports/profile-icons/`): Fastest outside-country→anti-censorship, Bypass profile→security, Home country profile→business, Streaming servers→streaming.

The upsell hero (`upsell-hero.jpg`, 661×1024) is the user-provided 3D ▽+ Proton Plus badge on a black background. `[UNVERIFIED]` It was supplied directly rather than exported from Figma — no dedicated hero node surfaced in the page metadata (only the Thumbnail frame). Swap for a higher-res Figma export if a node is provided.

## Reused from v1

- Leaflet dark tile layer (`dark_nolabels` CARTO) and imperative map setup
- Pulsating pin SVG (radial-ring + dot) and pin status colors (`#F7607B` / `#8882A0` / `#2CFFCC`)
- `StatusGradient` component (imported directly) for the coral/slate/teal top gradient crossfade
- Progressive asterisk scramble logic

## New in v2

- Framer Motion (`motion/react`) drives all screen transitions, layout morphs, staggered card rows, and the contextual-copy crossfade
- Animated sci-fi targeting reticle overlay (tracks the pin's screen position): 7 layers — breathing corner brackets, ticked crosshair, two counter-rotating dashed rings, a 60-mark graduated dial, pulsing directional arrows, and an orbiting radar scan sweep with a glow trail. Pure SVG + CSS keyframes; danger/coral palette only.
- Two-column JTBD workspace with Radix RadioGroup
- Live IP detection + per-country contextual copy
- Tuning loader + Tuned result screens (states 5–6) built from real Figma assets (nodes `19573-3917` / `19585-5045`), with staggered row entrance, green-check spring scale-pop, a top-left Back button (returns to JTBD), and a short hover Radix `Tooltip` on every (i) icon

## Design Tokens (from Figma `VPN | Windows | Explorations`)

- Window: `#16141c` bg, `#4a4658` border, rounded-8, mica drop shadow
- Desktop backdrop (behind the window, `OnboardingV2`'s outer wrapper): the native Windows wallpaper (`src/app/assets/windows-wallpaper.png`, shared with `App.tsx` — see `_overview.md`'s "Shared Architecture"), not a flat dark color
- Gradients: unprotected `rgba(247,96,123,0.5)`, connecting `rgba(255,255,255,0.5)`, protected `rgba(44,255,204,0.5)`
- Accents: coral `#f7607b`, teal `#2cffcc`, interaction purple `#6d4aff`
- Info card: `rgba(0,0,0,0.3)`, rounded-20, padding-16
- Type: Title 28/36 semibold, Body 16/20, all Segoe UI Variable
- JTBD option row: `rgba(255,255,255,0.05)` default / `rgba(255,255,255,0.2)` selected, rounded-12, padding-12; right panel border-2 white/20 rounded-40

## Content variants (Unprotected → Connecting → Protected)

The first three states support interchangeable content variants via the `variant` prop on `OnboardingV2` (`"v1" | "v2" | "v4" | "v4-split" | "hybrid" | "hybrid-split"`, **default `"hybrid"`** — both `OnboardingV2`'s own default and `App.tsx`'s initial `variant` state), selectable from the prototype-controls "Version" dropdown (grouped as `CONNECTION_GROUPS` — "#1 Location map spotlight" {`v1` Centered / `v2` Split}, "#2 Browsing experience" {`v4` Centered / `v4-split` Split}, "#3 Hybrid" {`hybrid` Centered / `hybrid-split` Split}). Onboarding now opens on "#3 Hybrid" / Centered by default; any group/layout is still freely selectable from the dropdown. v1–v2 share the map, pin, reticle, shield aura, gradients, and choreography (`ENTRANCE_TIMING`) and differ only in copy + info-card content; v4/v4-split are a self-contained, map-less concept variant (see below); Hybrid combines both, with its own Centered/Split layouts (see below). States 4+ (JTBD onward) are unchanged across all variants. **v3 ("Take back what's yours"), v5 ("The Watchers"), and v6 ("The Mirror") were retired** — their main title + subtitle (from v3) were ported into v4 before removal (see below); their component files were deleted.

- **v1 — Original**: threat framing. "Your online identity is currently unprotected" + live data card (country/IP/ISP) + "Protect my online identity".
- **v2 — "You're in control" (side control panel, `ControlPanelOverlay`)**: distinct **asymmetric layout** — a solid left rail (~400px, `rgba(22,20,28,0.85)` + blur) acts as a dashboard, and the map is **offset right** so the pin sits centered in the remaining region (`OnboardingMapV2.focusOffsetX`). Rail: padlock/spinner → headline → subtext → a "Visible on this network:" list of the exposure facts → CTA docked at the bottom. Copy: "Let's make this connection private" / "Setting up your private connection…" / "Done — you're private now". CTA "Make me private" → "Securing…" → "Continue".
- **v4 — "In Plain Sight" (open diary, `versions/v4-in-plain-sight/`)**: map-less single centered column. Everyday internet life logged as a diary. Main title + subtitle are **ported verbatim from the retired v3**: "Your online life is more visible than you'd think" / "On this network, others can see the everyday things you do." **5 curated, archetypal, relatable entries** (`data/activityEntries.ts`) type in one at a time (~42ms/char, blinking caret, ~1.1s apart, starting only after the header settles), each shows an eye + "Visible" — the app never reads real history. Entrance order: padlock → title → subtitle → diary entries one by one → CTA (`V4_TIMING.padlockAppear/headlineAppear/subtextAppear/feedStart`). The top status gradient is flipped (`scaleY(-1)`) to match the main app's convention, reading strongest at the top and fading downward. On connect entries redact oldest-first (asterisk scramble) with a synchronized `FeedScanline`, eyes → crossed-out eyes (a custom mark, not a Lucide icon — see `ActivityEntry.tsx`'s `EyeHiddenIcon`) with status "Hiding…" → "Hidden" (straightforward tone; other tones use their own words); protected act tints the mark teal, slides in a teal seal card ("From now on, this is all they see."), headline "Your diary is sealed." + confirmation footer. CTA "Make it unreadable.".
- **Hybrid — combined concept (`versions/hybrid/`)**: see "Version Hybrid" below.

Supporting changes: `InfoCard` gained optional `heading`, `footnote`, and per-row `icon` (`label` is now a `ReactNode`); `OnboardingMapV2` gained `focusOffsetX` (shifts the pin's on-screen position right for v2) and `focusOffsetY` (shifts it down/up, negative = up; used by Hybrid — see below); shared `Padlocks` component (`PadlockOpen`/`PadlockClosed`) extracted for reuse. `OnboardingV2` branches the states-1–3 overlay by variant (v1 inline centered layout; v2 dedicated overlay component; v4/Hybrid self-contained full-cover/transparent overlays) — the phase state machine, persistent map, connect timing, and states 4+ are shared and unchanged. v4 is map-less and renders an opaque backdrop that covers the persistent map during stage 1; Hybrid (like v1/v2) is a transparent overlay that lets the persistent map show through.

### Version v4 (new concept variant, no Figma)

v4 lives under `src/app/onboarding-v2/versions/v4-in-plain-sight/` and implements the shared `StageOneVersionProps` contract (`versions/types.ts`): `{ phase, geo, isLive, onProtect, onContinue, copy }`. It reuses the parent phase machine + 3.2s connect simulation (satisfies the ≥2.5s connecting hold; there is no real connect service, so no 15s timeout/retry exists to reuse). No i18n framework exists in the repo, so all copy is passed in via the `copy` prop (a `BrowsingCopy` object selected by tone — see "Tone of voice" below); `data/activityEntries.ts` keeps only the tone-invariant diary `ACTIVITY_ENTRIES` (icons/category/base text). No analytics infrastructure exists, so stage events are not fired. All diary content is archetypal (never the user's real history). Ships a `prefers-reduced-motion` variant (simple fades — no typewriter/scanline) and centralizes timing in `V4_TIMING`. Perpetual motion is CSS transform/opacity; one-time entrances use Framer Motion.

Shared version helpers (`versions/lib/`): `useReducedMotion`, `scramble.ts` (`useScramble`/`maskAll` — a local generalization of `useProgressiveMask`, kept separate so the shared hook is untouched), `ConnectionBackdrop` (reuses `StatusGradient`), `ConfirmationFooter` (VPN flag + country + VPN IP).

### Version Hybrid — combines map-spotlight + browsing experience

`versions/hybrid/` merges the two other groups into one three-act scene (Centered and Split layouts — `hybrid` / `hybrid-split`), reusing as much as possible rather than reimplementing: `Hybrid.tsx` (Centered orchestrator), `HybridSplit.tsx` (Split layout — same content/timing, different arrangement), `useHybridReveal.ts` (the card reveal/redact/connect-timeout sequencing shared by both layouts, since it's layout-independent), `LocationChip.tsx` (the one genuinely new visual element, reused by both layouts), `timing.ts` (`HYBRID_TIMING`). Both layouts take the same minimal props shape as v1/v2 (`{ phase, geo, isLive, onProtect, onContinue, tone }`), not `StageOneVersionProps` (they don't need a `BrowsingCopy` object — see "Tone of voice" below for how Hybrid gets its copy instead).

**What's reused, unchanged, and why nothing needed to be imported for it:** `OnboardingMapV2` (map, pulsating pin, `TargetingReticle`, `ShieldAura`, top `StatusGradient`) is rendered **once** by `OnboardingV2`, shared across every stage-1 variant and driven purely by the same `phase`/`geo`/`stage` values `OnboardingV2` already computes generically — Hybrid is simply another transparent overlay on top of it (like v1/v2), so the map/pin/reticle/aura/gradient/flyTo/spinner needed **zero new code**. `ActivityEntry` (from `versions/v4-in-plain-sight/`) is reused directly, unchanged, for the 3 activity cards — same component, same `useScramble`-driven redaction. Card **copy** is Hybrid-specific: `data/hybridActivityEntries.ts` (`HYBRID_ACTIVITY_ENTRIES` — broader category-level lines with Search/Globe/Smartphone icons), separate from v4's narrative `ACTIVITY_ENTRIES`. The real VPN "connect" is the same shared `handleProtect` in `OnboardingV2` every version already calls via `onProtect`.

**What's new:** `LocationChip` — a rounded-pill {flag} {country} · {IP} element. Act 1 shows the real (`useIpDetection`) country/IP, with a skeleton while `!isLive` (mirroring the same loading idiom `InfoCard`/`MaskedValue` use, without reusing that component's row-based markup, which doesn't fit a pill). Act 2 scrambles the country name and IP to asterisks via the same `useScramble` hook the cards use (200ms after connecting starts, so it reads as coordinated with the map's flyTo and the cards' redaction, not sequential); the flag fades rather than scrambling (nothing to redact in an icon). Act 3 crossfades the pill's *inner content* (not the pill container, which stays mounted throughout) to the resolved VPN identity from `VPN_SERVER` (country/flag/`vpnIp`, IP in teal) — `useScramble` can only mask/unmask its own input text, not resolve to a *different* value, so the value-swap is a straightforward AnimatePresence crossfade of the inner span, the same "persistent container, swapped inner content" idiom used elsewhere in this codebase (e.g. `InPlainSight`'s headline swaps). Also new: `useHybridReveal` (the small local reveal/redact-sequencing state for the 3 cards — `ActivityFeed`, the diary's own orchestrator, hardcodes all 5 entries with no count prop, so it wasn't reusable for "3 cards" as-is — reusing the card component directly and writing new, centralized sequencing for it is the explicitly-scoped-in "new orchestration" piece).

**Split layout (`HybridSplit.tsx`, key `hybrid-split`):** same content, data and `HYBRID_TIMING` pacing as Centered (via the shared `useHybridReveal`), rearranged into a left rail + right panel at an even **50/50 width split** (`w-1/2` each — a deliberate departure from the map-spotlight split's 400px/v2 and the browsing split's 380px rail widths, both narrower-rail conventions) — the left rail holds icon/title/subtext/**activity cards**/CTA vertically centered as one group (the cards sit between the subtext and the CTA, reserving a fixed `min-h-[164px]` — matching Centered's own reservation — so the CTA doesn't drift down as cards reveal progressively), and the right panel holds **only** the location chip, anchored in its **lower half** (the panel is split into two equal flex regions; the top is left empty for the pin, the chip is centered within the bottom one) so it reads as below the pin without needing to measure the pin's exact position. Unlike the browsing split (map-less, opaque), Hybrid's right panel stays **transparent** — the persistent map + pin show through, offset right via `OnboardingV2`'s `splitFocusOffsetX` (256px for the 50/50 rail, vs. v2's 200px for its 400px rail — both simplify to `railWidth / 2`, centering the pin in the remaining region) so the pin lands in that same visible area. No vertical pin *measurement* is attempted in Split (unlike Centered) — matching the map-spotlight split's own precedent, which doesn't center the pin against its rail content either; only Centered needs that, since only there does the pin sit in a dedicated gap between two specific elements.

**Continuity across acts:** the map is never unmounted (owned entirely by the parent, outside Hybrid); the chip is one persistent `motion.div` whose inner content crossfades; the 3 cards are the same `ActivityEntry` instances throughout (`key={entry.id}`, never phase-keyed) — only their `redact`/`sealed` props change. Icon and title/subtext use `AnimatePresence mode="wait"` keyed by phase for a true overlapping crossfade (a slightly higher-fidelity treatment than v1's own header, which fully remounts `key={phase}` on every transition).

**Opening entrance order (Act 1):** map pin (the shared map's own `ENTRANCE_TIMING.pinAppear`/`bracketsStart`, unchanged) → location chip settles → **beat (~450ms)** → activity cards cascade in (staggered ~175ms) → padlock icon → title → subtext → CTA. Timings live in `HYBRID_TIMING` (`chipEntranceDuration`, `beatAfterChipSettle`, `cardsStart`, `cardStagger`). Reduced motion: same content/order via simple fades, no choreographed beats (minimal 50ms card stagger).

**Pin vertical centering:** the title and subtext slots each reserve a fixed `min-h` (worst-case line count across all three acts' copy) so the block's bottom edge — and therefore the gap available for the map/pin below it — never shifts between acts (previously, hiding the subtext during Act 2, or a headline wrapping differently, would reflow everything below it). `Hybrid` then measures (via `getBoundingClientRect` + a `ResizeObserver`, `useLayoutEffect`) the actual gap between that stable header bottom and the top of the `LocationChip`, and reports the offset needed to center the pin in it through `onPinOffsetChange`, which `OnboardingV2` feeds into `OnboardingMapV2.focusOffsetY`. This is measured, not a hardcoded constant, so it stays correct regardless of exact font metrics or copy length.

**Tone of voice:** Hybrid is fully wired into the same axis as v1/v2/v4/v4-split (see "Tone of voice" below) rather than having its own dedicated, tone-agnostic copy. `CONNECTION_COPY[tone]` gained a third shape, `hybrid` (`HybridCopy`: `exposedHeadline`/`exposedSub`/`connectingHeadline`/`protectedHeadline`/`protectedSub`/`ctaProtect` — the map-spotlight-shaped half of the layout), alongside the existing `mapSpotlight` and `browsing`. The 3 activity cards' `visibleLabel`/`redactingLabel`/`sealedLabel` are **not** duplicated into `hybrid` — Hybrid reads those straight from `browsing` per tone, since the cards are the literal same reused `ActivityEntry` component. Card main-line text comes from `data/hybridActivityEntries.ts` (tone-invariant category copy). `straightforward.hybrid` preserves Hybrid's original headline/sub/CTA verbatim; the other three tones are fresh Hybrid-specific wording in the same voice as their `mapSpotlight` counterparts (not copy-pasted, to avoid two tones showing identical text within Hybrid itself).

**Known gaps, inherited from the source versions (not introduced by Hybrid):** no real connect service and no existing timeout/retry UI pattern exist anywhere in the repo to reuse — the simulated connect is a fixed ~3.2s `setTimeout`, so `HYBRID_TIMING.connectTimeoutMs` (15s) is a defensive hold-with-shimmer safeguard that can't currently fire, with no "Retry" action wired (there's no real failure to retry from). Neither `OnboardingMapV2` nor any existing version has reduced-motion handling for the map/flyTo itself; Hybrid's `prefers-reduced-motion` variant covers everything it controls (chip scramble, card scramble/typewriter, icon/title crossfades — via the shared `useReducedMotion`) but inherits the map's un-reduced flyTo, same as v1/v2 do today.

## Tone of voice (content axis — Stages 1 & 2)

A third prototype-controls dropdown, **"Tone of voice"**, rewrites copy **content only** — every layout, concept, animation and timing is untouched. It is orthogonal to the Version/Layout selectors, so any of the five connection layouts (Location map spotlight Centered `v1` / Split `v2`; Browsing experience Centered `v4` / Split `v4-split`; Hybrid Centered) — or, for stage 2, any of the 4 result layouts — can be rendered in any tone. The axis is `ToneOfVoice` (`"straightforward" | "reassuring" | "empowering" | "educational"`, `lib/toneOfVoice.tsx`), backed by a single **global** `tone` state in `App.tsx`, threaded down through the `tone` prop on `OnboardingV2` to whichever stage/variant is active. The dropdown is shown only for stages listed in `STAGE_SUPPORTS_TONE` — originally connection-only; **stage 2 ("tuning") now opts in too**, exactly per the pattern's own design intent ("the structure is deliberately generic so other stages can opt in later: add their copy shape here and add the stage to `STAGE_SUPPORTS_TONE`"). This confirms the selector was always meant to be one global control with per-stage opt-in visibility, not a separate mechanism per stage.

### Stage 1 (connection)

All connection copy lives in `CONNECTION_COPY[tone]`, split into `mapSpotlight` (headline/subtext/CTA/split-view card headings for `v1`/`v2`, with phase-colored accent spans and ISP interpolation), `browsing` (the `BrowsingCopy` object for `v4`/`v4-split`: headlines, sub, CTA, connecting/protected headlines, seal card, continue, `visibleLabel`, and the per-entry lock labels `redactingLabel`/`sealedLabel`), and `hybrid` (the `HybridCopy` object for Hybrid: `exposedHeadline`/`exposedSub`/`connectingHeadline`/`protectedHeadline`/`protectedSub`/`ctaProtect` — Hybrid's cards reuse `browsing`'s `visibleLabel`/`redactingLabel`/`sealedLabel` rather than duplicating them). `mapSpotlight`/`hybrid` `exposedSub`/`protectedSub` each supply **known** (named ISP + "others along the way/in between") and **unknown** (broad phrasing, no name) variants via `ispCopy()` + `resolveIspKnown(geo)` — honest broadening beyond ISP-only blame (provider, networks, sites visited). `OnboardingV2` reads `mapSpotlight` inline for `v1`/`v2`, passes `browsing` down as the `copy` prop to the diary components, and passes `tone` straight through to `Hybrid` (which reads `hybrid` + `browsing` itself); `ActivityFeed`/`ActivityEntry` take `redactingLabel`/`sealedLabel` as props (previously hardcoded "Protecting…"/"Protected"; the straightforward tone's values are now "Hiding…"/"Hidden", to match the crossed-out-eye icon rather than the earlier padlock-style wording). Hybrid card main-line text lives in `versions/hybrid/data/hybridActivityEntries.ts`; v4's tone-invariant diary entries in `activityEntries.ts` are unchanged.

The four tones:
- **Straightforward** (default — the original shipped copy): factual, calm, professional. e.g. "Your online identity is currently unprotected" / diary card mark "Hiding…" → "Hidden".
- **Reassuring**: warm, empathetic, peace-of-mind. e.g. "Right now, this connection isn't private" / CTA "Make my connection private" / lock "Securing…" → "Private".
- **Empowering**: agency and control. e.g. "You decide who sees you online" / CTA "Take control" / "You're back in control" / lock "Claiming…" → "Yours".
- **Educational**: expert clarity, explains the mechanism. e.g. "Here's what your network can see" / CTA "Encrypt my connection" / "Your traffic is now encrypted" / lock "Encrypting…" → "Encrypted".

### Stage 2 (tuning)

Every copy surface across the picker + tuned-result step is tone-aware: `JtbdGridPanel`'s title/subtitle; the tuned-result header's intro subtext, the "{verb} for {jtbd}…"/"{verb} for {jtbd}" title pair, and the completion summary; and every JTBD's 3 enabled + 2 paid feature **outcome** sentences and its **tip**, across all 4 layouts and all 6 JTBDs.

**Structural copy** — `TuningCopy` (`lib/toneOfVoice.tsx`), a `Record<ToneOfVoice, TuningCopy>` named `TUNING_COPY` (unrelated to, and a full replacement for, the *different*, now-deleted `TUNING_COPY` that used to live in `versions/lib/tuning.ts` before the tuning-stage consolidation — pure naming coincidence, no shared history):
```ts
interface TuningCopy {
  pickerTitle: string;
  pickerSubtitle: string;
  introSubtext: string;
  titleDuring: (jtbdWord: string) => string;
  titleComplete: (jtbdWord: string) => string;
  summarySubtext: (applied: number, locked: number) => string;
}
```
`JtbdGridPanel` reads `pickerTitle`/`pickerSubtitle` directly from `TUNING_COPY[tone]`. `tuned-result/copy.ts`'s `introSubtext`/`titleDuring`/`titleComplete`/`summarySubtext` functions were changed to take a `tone` parameter and delegate into `TUNING_COPY[tone] ?? TUNING_COPY.straightforward` (the fallback-to-straightforward on a missing tone every tone-aware function in this stage implements). `counterSubtext` ("Applying X of N settings") took no `tone` parameter change — it's one of the strings kept constant (see below).

**Per-JTBD copy** (outcomes + tips) — a *separate* file, `lib/jtbdTuningToneCopy.ts`, a sibling of the base `lib/jtbdTuningResult.ts` it re-voices: `JTBD_TONE_OUTCOMES: Record<ToneOfVoice, Record<JtbdId, JtbdToneOutcomes>>` where `JtbdToneOutcomes = { enabled: [string,string,string]; paid: [string,string]; tip: string | null }`, mirroring the base data's tuple shape/order exactly. Deliberately NOT folded into `toneOfVoice.tsx` — cramming 6 JTBDs' worth of outcome sentences into that file would break the "one file per content concern" convention every other data file here follows (`jtbdData.ts`, `jtbdUpsell.ts`, etc.); `straightforward`'s entries are *built directly from* `JTBD_TUNING_RESULT`'s existing `outcome`/`tip` fields (not hand-copied) so the two can never drift. Two lookup helpers, `toneOutcome(tone, jtbdId, "enabled"|"paid", index)` and `toneTip(tone, jtbdId)`, both fall back to `straightforward` on a missing tone. The base data (`settingsName`/`value`/`featureName`/`asset`/`tooltip`) is **never** re-voiced or touched — only the plain-language `outcome`/`tip` text changes.

**Wiring into the layouts:** all 4 (`StackedLayout`/`CompactListLayout`/`SplitByStatusLayout`/`CardGridLayout`) gained a `tone` prop. `StackedLayout` (whose resolved row JSX is bespoke, not `EnabledFeatureRow`/`PaidFeatureRow`) calls `toneOutcome(...)` directly in place of `feature.outcome`. The other three reuse `EnabledFeatureRow`/`PaidFeatureRow` verbatim (unmodified, per scope) — since those components read `outcome` straight off the `feature` object passed to them, each layout builds a `{ ...feature, outcome: toneOutcome(...) }` shallow copy immediately before rendering, so only the outcome text is swapped and every other field (settings name, value, feature name, asset, tooltip) passes through completely untouched. The tip lookup (`toneTip`) exists as data for completeness (the task's copy matrix explicitly asked for it) but isn't rendered by any current layout — matching the base `JTBD_TUNING_RESULT.tip` field's own status, which was already unused after the tip footnote was removed from every result screen (see below); reintroducing a Tip UI was out of scope for this change.

**Confirmed CONSTANT across all 4 tones** (checkpoint): the 6 JTBD option labels (`JTBD_OPTIONS`), technical settings names/values/feature names/decorative assets (all of `JTBD_TUNING_RESULT` except `outcome`/`tip`), the "Available with VPN Plus" badge + Split by Status column headers (`TUNED_RESULT_COPY`), the counter format ("Applying X of N settings"), the picker's witty "wink" line (`JTBD_WINK_COPY`), the per-row narration verbs ("Enabling…"/"Checking…", `narrateEnabling`/`narrateChecking`), and the neutral navigation labels (Skip, Back, the pre-selection "Continue", and the dynamic "Tune for {jtbd}" CTA, `JTBD_CONTINUE_LABEL`) — this last group mirrors stage 1's own precedent, where `browsing.continue` is likewise the literal string `"Continue"` in all 4 tones; only the more expressive/emotive strings vary by tone in either stage.

**Analytics:** no analytics infrastructure exists anywhere in this repo (confirmed again during this change) — consistent with every other tone-aware surface, no events are fired for tone changes.

## Tuning-stage: single consolidated flow + result layouts

The "Personalized JTBD tuning" stage was **consolidated from 5 separate versions into ONE flow**, formerly the "Visual Tuning" version's mechanics — picker → no separate loader → a result step that opens with its own centered intro and materializes its 5 settings one at a time. The 5 selectable JTBD-tuning versions that used to exist (Default, Loadouts, Split by Status, Card Grid, Visual Tuning) are gone as *versions*; three of their result designs survive as **result LAYOUTS** of the one consolidated flow — Loadouts did not survive in any form (it was a fully self-contained morphing-card scene with no reusable result "arrangement" concept, and nothing else in the codebase depended on it or its `TuningStageVersionProps` contract, confirmed before removal). The bottom prototype selector that used to pick a "Version" for this stage now picks this **Layout** instead.

**Files removed in the consolidation:** `JtbdPanel.tsx` (the old two-column radio-list picker — `JtbdGridPanel` is now the stage's only picker), `components/TuningLoaderState.tsx` (the separate "Tuning the app for you…" loader — no longer used by anything), `components/TunedResultState.tsx` (the old "Default" result screen), `versions/tuning-loadouts/` (entire folder), `versions/tuning-split-by-status/`, `versions/tuning-card-grid/`, `versions/tuning-visual-tuning/` (the latter three's designs live on as `tuned-result/layouts/*` instead — see below), and `versions/lib/tuning.ts` (`TuningStageVersionProps`/`JTBD_ORDER`/`TUNING_COPY`, used only by Loadouts). `EnabledFeatureRow`/`PaidFeatureRow` were **kept unchanged** — they're still reused both by the new layouts and by the untouched, downstream `PlusWelcomeState`.

**The tip footnote was removed from every result-style screen** (`TunedResult` — all 4 layouts — and `PlusWelcomeState`, since it re-shows the same content). `components/TuningTip.tsx` was deleted (it had no other callers left); `JTBD_TUNING_RESULT`'s `tip` field is left in the data unchanged (still populated per-JTBD) but is simply never read by any component now. `useTunedMaterialization`'s `continueDelayMs` no longer waits on a tip gap — Continue fades in directly after the last item resolves.

### The single flow

1. **Picker** (`JtbdGridPanel`, phase `jtbd`) — unchanged from before this consolidation (3×2 grid, wink message, dynamic Continue label, toggle-to-deselect). `onContinue` now unconditionally advances straight to phase `tuned` — there is no `tuning` (loader) phase left in the `Phase` type at all.
2. **Result step** (`TunedResult`, phase `tuned`, `src/app/onboarding-v2/tuned-result/TunedResult.tsx`) — the sole result component, parameterized by a `layout: ResultLayout` prop (`"stacked" | "split-by-status" | "card-grid" | "compact-list"`, default `"stacked"`). Runs, identically for every layout:
   - **Centered intro** — the header block (loader `Spinner` + "Tuning for {jtbd}…" + a static "Applying the settings to optimize the experience for you." subtext) fades in centered on screen and holds there for `centerHold` (2000ms).
   - **Move to top** — the SAME block (one persistent `motion.div`, Framer's `layout` prop drives the FLIP from a `justify-center`→`justify-start` wrapper toggle, never unmounted) travels to its top position over `moveToTop` (600ms); the spinner crossfades (true overlap, not sequential) into the JTBD's category icon (`JTBD_ICONS`, same fixed 48×48 slot, no size jump) and the subtext crossfades to the live "Applying {X} of {N} settings" counter. Reduced motion skips the travel animation entirely (renders top-anchored from the start; `layout` disabled) and only holds briefly (`reducedIntroHold`, 500ms) before the same crossfades fire.
   - **Materialization** — only once the block has landed do the 5 settings items begin their two-phase reveal, one at a time (Phase 1: narrated spinner "Enabling {setting}…"/"Checking {feature}…"; Phase 2, ~300ms: resolves into the finished visual). The counter's X is `rowMounted.filter(Boolean).length` — derived directly from the same row-progression state the items render from (no separate counter variable), incrementing the instant each item's Phase-1 spinner is scheduled (both free and locked items count, 1..5). A free/paid **boundary beat** (an extra pause, `boundaryIn + rowGap`) is reserved before the first locked item begins in every layout's timing, even layouts (Card Grid) that don't render a visible widget there — this keeps total pacing identical across all 4 layouts, per the "centralize timing in one shared config" requirement.
   - **Completion** — once the last item resolves, the title crossfades to "Tuned for {jtbd}" (same sentence structure as the "during" form, just the verb/ellipsis change, so it reads as a simple word swap) and the subtext to the derived "{applied} settings applied · {locked} available with VPN Plus" summary (the "· {locked}…" clause is dropped entirely, not shown as "0 available", for Plus users); Continue fades in and becomes interactive. No tip footnote anywhere in this step (removed across all 4 layouts — see below). `{jtbd}` is `result.jtbdLabel.toLowerCase()` throughout — already the project's agreed gerund short form for bypass ("Bypassing restrictions"), so no separate lookup table is needed.
3. `OnboardingV2` keys `<TunedResult key={resultLayout} .../>` by the selected layout, so **switching the Layout selector remounts the whole component and replays the sequence from the centered intro** in the newly chosen arrangement — confirmed as the desired prototype behavior (so reviewers can see the full intro + materialization in any layout, not just its static end state), rather than jumping straight to a completed state.

Only the RESOLVED ARRANGEMENT of the 5 items — and which "shape" their Phase-1 placeholder takes — differs per layout; the intro, header move, counter, completion strings, and Continue are 100% shared, driven by one hook (`useTunedMaterialization`) and one timing config (`TUNED_RESULT_TIMING`, in `tuned-result/timing.ts`) with no per-layout timing overrides.

### `tuned-result/` file structure

| File | Purpose |
|------|---------|
| `TunedResult.tsx` | Orchestrator — the header intro/move/counter/completion sequence (identical for every layout) + dispatches the items area to one of the 4 `layouts/*` components based on `layout` |
| `useTunedMaterialization.ts` | The shared row-by-row timing/state hook (`introDone`, `rowStages`, `rowMounted`, `boundaryVisible`, `appliedSoFar`, `rowsComplete`, `continueDelayMs`) — layout-independent; only rendering differs per layout, not this hook |
| `timing.ts` | `TUNED_RESULT_TIMING` — the one config shared by all 4 layouts (intro hold, move, per-item cadence, boundary pause, completion, Continue gap) **and** stage 3's locked→unlocked transition (`unlockSettleDelay`/`unlockConfettiDelay`/`unlockChipFadeMs`/`unlockTransformStagger` — moved here from local consts in `PlusWelcomeState.tsx`, values unchanged, now shared by every layout's transform) |
| `copy.ts` | `TUNED_RESULT_COPY` (plusHeader/continue/introSubtext/column headers) + `titleDuring`/`titleComplete`/`counterSubtext`/`summarySubtext` (derived-string helpers) + `narrateEnabling`/`narrateChecking` (per-setting/feature narration, with a generic fallback) |
| `MaterializingSlot.tsx` | The generic two-phase (`spinner`→`resolved`) crossfade wrapper every layout's items use — takes a `phase1Content`/`resolvedContent` pair per item; `resolvedContent` is mounted fresh at resolve time inside an `initial="hidden" animate="show"` context so components with their own `variants` (e.g. `EnabledFeatureRow`'s check-pop) fire naturally with no extra wiring. Its internal `AnimatePresence` children (and the reduced-motion wrapper) are explicitly `w-full` — for a row-direction flex caller (e.g. Stacked's `ROW_CLASS`), a flex item with no declared width shrinks to its content instead of stretching to fill the row, which silently starves any `flex-1` element inside `resolvedContent` (like the outcome sentence pushing a pill to the row's far right) of the space it needs, leaving the pill glued next to the text instead of at the true right edge — a bug fixed once here, benefiting every layout that reuses this component. |
| `PhaseOnePlaceholder.tsx` | The shared spinner+narration markup shown during Phase 1 (`"row"` or `"block"` arrangement) |
| `BoundaryDivider.tsx` | The free/paid divider + "Available with VPN Plus" badge widget shared by Stacked and Compact List (not used by Split by Status, which has its own column headers, or Card Grid, which has none) |
| `TransformingPaidCell.tsx` | Stage 3 only — the reused locked→unlocked transition (badge↔check crossfade, dim→full opacity/color, "Just unlocked" chip, and, `"row"` shape only, the "Available" pill fly-in) generalized across `EnabledFeatureRow`/`PaidFeatureRow`'s 3 container shapes (`layout: "row" \| "stacked" \| "card"`), used by Compact List (`row`), Split by Status (`stacked`), and Card Grid (`card`). Stacked's own resolved shape is bespoke (merged pill, no card), so it has its own inline transform instead — see below. Exports `UnlockedChip` (the "Just unlocked" chip), reused by that inline transform too. |
| `layouts/StackedLayout.tsx` | **Stacked** (default) |
| `layouts/CompactListLayout.tsx` | **Compact List** |
| `layouts/SplitByStatusLayout.tsx` | **Split by Status** |
| `layouts/CardGridLayout.tsx` | **Card Grid** |

### The 4 layouts

**Naming note:** the internal layout identifiers below (Stacked/Compact List/Split by Status/Card Grid — component names, `ResultLayout` values, doc section names) are unchanged, but the prototype-controls "Layout" dropdown shows different, friendlier labels for the first 3: Stacked → **"Minimal list"**, Compact List → **"Richer list"** (reordered to sit right after Minimal list, since it's the natural "more detail" step up), Split by Status → **"Split view"**; Card Grid's label is unchanged. `STAGE_VERSIONS.tuning`/`.upgrade` (`OnboardingV2.tsx`) list them in this order: Minimal list, Richer list, Split view, Card Grid.

- **Stacked** ("Minimal list" in the Layout dropdown; `layouts/StackedLayout.tsx`, key `stacked`, **default**) — the former "Visual Tuning" version's exact row style, ported verbatim: full-width rows, no per-row background; each resolves to a green check + benefit sentence + a single merged "{settingsName}: {value}" pill (a bespoke visual — not one of `EnabledFeatureRow`'s existing `layout` modes, since none of those merge the label into the value pill); locked rows resolve to a plain custom "unavailable" glyph (`CircleSlashIcon`, a specific requested mark, not a `lucide-react` icon) + dimmed sentence + dimmed "{asset} {feature name}" pill (no value). A divider + "Available with VPN Plus" header (`BoundaryDivider`) animates in at the shared boundary beat; omitted entirely for Plus users (no tier boundary to mark when all 5 items are active).
- **Compact List** ("Richer list" in the Layout dropdown; `layouts/CompactListLayout.tsx`, key `compact-list`) — reuses `EnabledFeatureRow`/`PaidFeatureRow`'s existing, unmodified `layout="row"` mode verbatim for the resolved visual (separate settings-name label + value pill — the former "Default" version's original look, distinct from Stacked's merged pill) — only the Phase-1 spinner+narration placeholder (shaped to match that row's outer classes) is new. Gets the same `BoundaryDivider` beat as Stacked (a deliberate, shared "one-by-one" treatment, unlike the former "Default" version which had no divider at all). Each row's `MaterializingSlot` wrapper is a plain block `w-full` (not `flex`/`justify-center`) — a block element's width always resolves to exactly 100% of its parent regardless of its content, so the loading placeholder and the resolved row are guaranteed identical widths across every row, with no dependency on flex main-axis distribution (which previously let some rows shrink to fit their content instead of matching their siblings).
- **Split by Status** ("Split view" in the Layout dropdown; `layouts/SplitByStatusLayout.tsx`, key `split-by-status`) — the free/paid boundary is spatial (two columns, left "Active now" / right "With Plus" or "Also active with Plus" for Plus users) rather than sequential; the 5 items still materialize in the SAME underlying 0..4 order the shared hook drives — "one by one" here means each item resolves into its own column slot. The left column header appears with the first item (materialization start); the right column header at the shared boundary beat. Resolved visuals reuse `EnabledFeatureRow`/`PaidFeatureRow`'s existing `layout="stacked"` mode verbatim.
- **Card Grid** (`layouts/CardGridLayout.tsx`, key `card-grid`) — cards materialize in reading order: the 3 applied cards (top row, `grid-cols-3`) first, then the 2 locked cards (bottom row, `grid-cols-2`, ~1.5× an applied card's width). The row separation itself stands in for a divider — no separate boundary widget is rendered (unlike the other 3 layouts), though the shared timing still reserves the same pause there for pacing consistency. Resolved visuals reuse `EnabledFeatureRow`/`PaidFeatureRow`'s existing `layout="card"` mode verbatim. Responsive breakpoints (2-col at ~900px, 1-col at ~560px) carried over unchanged from the former "Card Grid" version.

**Plan-aware (unchanged behavior, carried over from the former "Visual Tuning" version):** for Plus users, the 2 locked items instead materialize as free-style items ("Enabling {featureName}…" → check + outcome + plain asset+name, matching `PaidFeatureRow`'s existing `unlocked` convention — no fabricated value, since `PaidFeature` has none) in every layout; Stacked/Compact List omit their boundary widget entirely, Split by Status relabels its right header ("Also active with Plus") rather than hiding it, Card Grid is unaffected (it never had a widget to hide).

**Reduced motion (shared across all 4 layouts):** no spinner phase at all — items appear directly in their resolved state via a plain sequential opacity fade (`MaterializingSlot`'s reduced path, no slide); the header skips its travel animation (see above); the boundary widget (where one exists) fades without the scale/expand. `EnabledFeatureRow`/`PaidFeatureRow`'s own internal entrance/pop variants aren't reduced-motion aware (a pre-existing trade-off, not introduced by this consolidation — suppressing them would need either a further prop or forking their content logic).

### Stage 3 reuses stage 2's layouts (shared "Layout" selector)

Stage 3's Plus-welcome result (`PlusWelcomeState`, state 9) renders in the SAME 4 layout components as stage 2's `TunedResult`, fed a `layout: ResultLayout` prop — **no separate selector**: `resultLayout` is (and always was) a single state lifted to `App.tsx`, threaded through `OnboardingV2` to both `TunedResult` and `PlusWelcomeState` — the prototype-controls "Layout" dropdown now appears (and is enabled) for both the "tuning" and "upgrade" stages, changing the same underlying value from either one (`STAGE_VERSIONS.upgrade` mirrors `STAGE_VERSIONS.tuning`'s 4 options; `usesResultLayout` in `App.tsx`'s `PrototypeControls` covers both stages).

Unlike stage 2, `PlusWelcomeState` skips materialization entirely — all 5 items are passed as already `"resolved"` (`rowStages`/`rowMounted` are constant arrays, not driven by `useTunedMaterialization`) — and passes each layout a new optional prop, `unlockTransition?: { unlocked: boolean; showChip: boolean }`. This is the ONLY behavioral branch added to the 4 layout components; when omitted (stage 2's call sites), behavior is byte-for-byte unchanged. When present:

- All 3 free items render in their normal active state (unaffected).
- The 2 paid items render via `TransformingPaidCell` (row/stacked/card variant matching that layout's own resolved shape) instead of the static `paidUnlocked`-branched JSX — this is what makes the locked→unlocked crossfade possible; the static branch (still used by stage 2 for a hypothetical `userPlan="plus"`) has no live-transition capability, since it's a fixed final render chosen once at mount, not something a boolean toggle can animate between.
- Stacked's own paid row is bespoke (not one of `EnabledFeatureRow`/`PaidFeatureRow`'s 3 shapes), so it has its own equivalent inline transform in `StackedLayout.tsx` (same choreography/timing, reusing `TransformingPaidCell`'s exported `UnlockedChip`).
- Split by Status's right-column header ("With Plus" ↔ "Also active with Plus") tracks `unlockTransition.unlocked` live, not the settled `paidUnlocked`, so it updates in sync with the 2 tiles' own transition rather than showing the final state from the first frame.
- `paidUnlocked` is passed as `true` (the settled/eventual value — every layout's boundary-widget-hiding logic, e.g. Stacked/Compact List's divider, already reacts correctly to a Plus user) while `unlockTransition.unlocked` is the separate LIVE, animated value driving the crossfade itself.

`PlusWelcomeState` owns only what's genuinely stage-3-specific: the "Welcome to VPN Plus" / JTBD subheading header, the confetti (`Confetti.tsx`, unchanged), the timers driving `unlocked`/`showConfetti`/`showChip` (now reading `TUNED_RESULT_TIMING`'s `unlock*` constants instead of local consts), and the "Start using VPN Plus" CTA. Everything about how the 5 items are arranged and how the 2 paid ones visually transform is delegated to the reused layouts + `TransformingPaidCell`.

`OnboardingV2` keys it `<PlusWelcomeState key={resultLayout} .../>` — the same convention `TunedResult` already uses for the shared selector — so switching "Layout" while stage 3's result is on screen remounts the whole component: the confetti burst and the 2 Plus items' locked→unlocked transition always replay from the beginning in the newly selected arrangement, instead of the layout swapping under an already-settled/unlocked state.

## Multiple-mode tuning ("Selection" prototype control)

A prototype-only **"Selection"** dropdown (Single / Multiple, `App.tsx`, shown pre-start and for the "tuning" stage) lets the JTBD picker select more than one intent. **Multiple is the default prototype experience** (`App.tsx`'s `selectionMode` state starts as `"multiple"`, confirmed) — Single remains fully supported and selectable from the same dropdown, and every underlying component's own prop default is still `selectionMode = "single"` (`JtbdGridPanel`, `TunedResult`), so nothing about Single mode's behavior changed, only which mode this top-level state starts as. Every Multiple-mode code path is gated behind `selectionMode === "multiple"` and, further, behind `selectedJtbds.length >= 2` (with exactly 1 selection, Multiple mode renders identically to Single mode — confirmed design: "1 selected → exactly as today" for every phase of this screen).

**Conflict analysis (confirmed at checkpoint):** enumerating every `enabled` setting across all 6 JTBDs, exactly ONE settings name is ever contributed with a genuinely different value by two JTBDs — `"LAN setting"` (privacy: "Blocked", travel: "Off", both meaning "LAN access disabled"). Every other setting that co-occurs across JTBDs (Kill Switch, NAT type, Smart Protocol, Stealth protocol, Encrypted connection) already uses an identical value everywhere it appears. `SETTING_CONFLICT_RESOLUTION` (`lib/jtbdMerge.ts`) is therefore a minimal, confirmed map (`"LAN setting" → "Off"`) rather than a full strictness-ranking table — extend it there if a future JTBD/setting introduces a genuine conflict.

**The picker (`JtbdGridPanel.tsx`):** new optional props `selectionMode`/`selectedMultiple`/`onToggleMultiple`, all defaulted so Single mode's own `selected`/`onSelect` path is untouched. Multiple mode: tiles toggle in/out of an ordered `selectedJtbds: JtbdId[]` (state lives in `OnboardingV2`, `toggleSelectedJtbd`); title/subtitle become "Pick what matters to you" / "Choose as many as apply. We'll tune Proton VPN for all of them." (`TUNING_COPY[tone].pickerTitleMultiple`/`pickerSubtitleMultiple`, tone-aware with the same `?? straightforward` fallback pattern as every other tone-aware string here — only `straightforward` has distinct content, confirmed at checkpoint as acceptable since full 4-tone variants for these were out of scope); the wink line shows the LAST-selected JTBD's line, fading out at 0 selections; the CTA is "Continue" (0 selected) / the existing `JTBD_CONTINUE_LABEL[jtbd]` (1 selected) / `"Tune for {count} interests"` (2+, confirmed phrasing). Each tile (`JtbdGridTile.tsx`) also gains a `multiple` prop (`false` in Single mode, unchanged there) that renders a top-right checkbox indicator: unselected — a plain ring (`rgba(255,255,255,0.35)` border, transparent fill); selected — a filled purple (`#6d4aff`) circle with a white `lucide-react` `Check` glyph, matching the tile's own selected-border accent color.

**The merge engine (`lib/jtbdMerge.ts`):**
- `mergeFreeSettings(selectedJtbds)` — UNION of the selected JTBDs' `enabled` settings, deduplicated by `settingsName` via a `Map` (which preserves insertion order): each name is only ever INSERTED on its first encounter (so row order is "first-selected JTBD's rows in its own data order, then new unique rows from the second-selected, and so on", and the row's `outcome` text always stays the earliest-selected contributor's — this is what "the outcome sentence uses the earliest-selected contributor" means in code); later JTBDs contributing an already-seen setting can only affect its `value` via `SETTING_CONFLICT_RESOLUTION`. Each returned `MergedEnabledFeature` also carries `primarySourceJtbd`/`primarySourceIndex` (for tone-outcome lookup) and `sourceJtbds` (informational). This is always the FULL, uncapped union — see "Result curation" below for what trims it for display.
- `mergePaidFeatures(selectedJtbds)` — the same UNION/dedup convention as `mergeFreeSettings`, for the selected JTBDs' 2 `paid` features each (deduplicated by `featureName`; no conflict table needed — paid features have no `value` field to conflict over). Returns `MergedPaidFeature[]` (`sourceJtbds`/`primarySourceJtbd`/`primarySourceIndex`, same shape as `MergedEnabledFeature`).
- `buildProfilePreviews(selectedJtbds)` — one `ProfilePreview` (`jtbd`, `label` from `JTBD_PROFILE_LABEL`, `icon` from `JTBD_ICONS`) per selected JTBD, in selection order.
- `rankFreeSettings(merged)` / `rankPaidFeatures(merged)` — sort the full merged unions by the editorial `SETTINGS_RANK`/`FEATURES_RANK` maps (`lib/jtbdTuningResult.ts`, lower number = higher priority; confirmed at checkpoint — see "Result curation" below for the full order). `capList(ranked, cap)` then takes the top `cap` items and returns `{ displayed, overflow }` — the single shared truncation step both the free and Plus lists use.
- `outcomeForEnabled(tone, result, index, feature)` / `outcomeForPaid(tone, feature)` — the tone-voiced outcome lookups used by rendering; for a plain single-mode feature they're byte-for-byte the old `toneOutcome(tone, result.jtbdKey, kind, index)` call; for a merged feature they look up `toneOutcome(tone, feature.primarySourceJtbd, kind, feature.primarySourceIndex)` instead, since a merged row's index in the MERGED array is meaningless for tone lookup.
- `TuningResultLike` — a relaxed, structurally-compatible view of `JTBDTuningResult` (`enabled`/`paid` as plain arrays instead of fixed 3-/2-tuples) that all 4 layouts' `result` prop now accepts — tuples are assignable to arrays, so Single mode's real per-JTBD data needs no change; Multiple mode passes a synthetic `{ ...single, enabled: freeCapped.displayed, paid: [] }` (the CAPPED free list, not the full union).

**Result curation (capped/ranked lists — screen height stays stable from 1 to 6 selected interests):** the merged free-settings union can grow to 10 unique rows and the merged paid-features union to 12, across all 6 JTBDs. Rather than rendering the full union (which grew the screen unboundedly), `TunedResult` ranks each union (`rankFreeSettings`/`rankPaidFeatures`) and caps the DISPLAY to the top items by editorial priority (`TUNED_RESULT_TIMING.freeRowCap` = 4, `paidFeatureCap` = 1 — tunable config, confirmed at checkpoint alongside the ranks themselves). The Plus section's total row count is therefore always exactly 2 — the profiles-summary row + 1 feature row (confirmed at checkpoint: "profile and something else depending on the selection").

**Completion counts match the DISPLAYED rows, not the true merged-union totals** (confirmed at a checkpoint, superseding this doc's earlier "always true totals" rule): the subtext's `{free} settings applied` is always exactly `freeRowCap` (4) once 2+ interests are selected — never the (potentially much larger) full merged-union size. The `{features} features with VPN Plus` count is every row actually shown in the Plus section — the capped feature row(s) (`paidFeatureCap`, 1) PLUS the profiles row itself (always exactly 1 row, however many interests it lists) — so with the current cap this is always **2** (1 feature row + 1 profiles row), confirmed at checkpoint. Pluralization is handled (`summarySubtextMultiple`, `lib/toneOfVoice.tsx`) for the (now largely theoretical, since the count is always 2 today) singular case.
  - **`SETTINGS_RANK`** (`lib/jtbdTuningResult.ts`, confirmed order): Kill Switch (1) → Smart Protocol (2) → Stealth protocol (3) → Alternative Routing (4) → Encrypted connection (5) → NAT type (6) → Hidden IP (7) → LAN setting (8) → WireGuard Kernel (9) → Device support (10). Protection-critical settings rank highest; niceties lowest.
  - **`FEATURES_RANK`** (confirmed order): NetShield (1) → Secure Core (2) → Streaming servers (3) → VPN Accelerator (4) → Server breadth (5) → P2P servers (6) → Port Forwarding (7) → Moderate NAT (8) → Fastest outside-country (9) → Network warning (10) → Home country profile (11) → Bypass profile (12).
  - **Completion counts match what's displayed** (see the checkpoint note above this list) — the subtext's `{N} settings applied`/`{M} features` are the DISPLAYED/capped counts, same as the live "Applying X of N" counter during materialization (`totalRows` = capped free + 1 profiles-summary row + capped Plus features) — both narrate what's actually on screen, not the full merged-union size.
  - **No overflow footnote at all** (confirmed at checkpoint, both sections): earlier iterations of this design appended a muted "+X more…" line whenever a union exceeded its cap (`MoreRow.tsx`, since removed). Neither the FREE section nor the PLUS section shows this anymore — anything beyond the top 4 free settings (`SETTINGS_RANK`) or the 1 Plus feature (`FEATURES_RANK`) is simply not mentioned in the list. The completion subtext above the lists is still the true, honest total (see above) — only the per-section footnote was removed, not the honesty guarantee.

**The Plus section (broadened beyond profiles-only):** the old "one full preview card per selected interest" design over-indexed on profiles and is replaced by two compact sub-parts under the "Available with VPN Plus" boundary:
  1. **Profiles row** (`ProfilesSummaryRow.tsx`, `layout: "row" | "stacked" | "card"` matching each result layout's tile shape) — the exact same "unavailable" `CircleSlashIcon` glyph (extracted to its own shared file, reused by Stacked's own hand-rolled locked rows too) + the fixed intro text "One-click profiles for your interest" (`PROFILES_INTRO_TEXT`, `tuned-result/copy.ts`, tone-constant) on the left, and ONE tangible pill per selected interest on the right — intent icon (`ProfilePreview.icon`, the same JTBD category icon used by the picker) + name, styled as an `asset + label` pill identical to the sibling paid-feature rows' own right-side featureName pill in its LOCKED state (dimmed icon, muted text — confirmed at checkpoint; never the bold/"active" treatment). The `layout="row"` variant (used by Stacked/Compact List) has no outer border/background box, matching Stacked's own boxless free/paid rows exactly (confirmed at checkpoint); the `"stacked"`/`"card"` variants keep their bordered box, matching Split by Status/Card Grid's own bordered `PaidFeatureRow` tiles. The `"row"` variant keeps the intro text on the left and the pills on the right (confirmed at checkpoint — not stacked): the text is `shrink-0`/`whitespace-nowrap` so it always keeps its natural width regardless of pill count (the earlier bug had the pills' `flex-shrink-0` force the text into an awkward multi-line squeeze instead), while the pills sit in the remaining space (`flex-1`, `justify-end`) and `flex-wrap` onto additional lines — each wrapped line still right-aligned, since `justify-content` on a wrapping flex container applies per line — once they no longer fit on one. Collapses what used to be up to 6 full cards into one compact row that still shows every pick as a tangible item, without dominating the section.
  2. **Top locked/Plus features, capped and ranked** — the deduped `mergePaidFeatures` union, ranked by `FEATURES_RANK` and capped at `paidFeatureCap` (1), rendered with each layout's OWN existing paid-row visual language (`PaidFeatureRow` for Compact List/Split by Status/Card Grid; Stacked's own hand-rolled locked-row style for Stacked) — i.e. the exact same locked-row look Single mode already uses, just fed merged/capped data instead of one JTBD's 2 features.
  **Honesty:** the feature rows still say "available"/render Plus-active through the existing `unlocked` prop exactly as Single mode's locked rows always have; nothing is created or usable for free users. The profiles row uses the same "unavailable" framing as every other locked row on this screen (no separate ▽+ badge/caption needed — the left glyph already communicates it).

**Materialization (reuses `useTunedMaterialization` unchanged, just fed different counts):** `TunedResult` computes `totalRows` as capped-free-rows + 1 (the profiles-summary row) + capped-Plus-feature-rows, and `boundaryIndex` at the end of the capped free rows — same shared timing/state hook, no changes to its logic. Because Multiple mode's synthetic `result.paid` is always `[]`, the 4 layouts' EXISTING paid-row rendering functions simply no-op; each layout adds a `renderProfilesSummaryRow`/`renderPlusFeatureRow` pair (using the SAME shared row-index space the boundary/materialization schedule reserves for the "paid slice") instead. **Pacing guard:** `useTunedMaterialization` compresses `spinnerHold` to `TUNED_RESULT_TIMING.spinnerHoldCompressed` (700ms, from 1000ms) once `totalRows > TUNED_RESULT_TIMING.pacingGuardRowThreshold` (6) — with the caps in place this rarely triggers in practice (max `totalRows` is now 5+1+4=10, only just above the threshold at the highest selection counts), and Single mode's `totalRows` is always 5, so it never activates there.

**Header (2+ selected):** icon crossfades from the loader spinner into a horizontal row of the selected JTBDs' picker icons (`JTBD_ICONS`, same SVG assets as `JtbdGridTile`, one per interest, left-to-right in selection order, 32×48px each with 8px gap, subtle ~70ms/icon left-to-right stagger layered inside the existing `iconCrossfade` — disabled under `prefers-reduced-motion`); title/subtext use `titleDuringMultiple`/`titleCompleteMultiple`/`summarySubtextMultiple` (`tuned-result/copy.ts`, same tone-fallback pattern as the picker's strings above) — "Tuning for {count} interests…" → "Tuned for your {count} interests" → "4 settings applied · 2 features with VPN Plus" (the "features" count is every row shown in the Plus section — capped feature row(s) + the profiles row itself, confirmed at checkpoint; not just paid features). No tip is shown in Multiple mode, consistent with Single mode's own current behavior. With exactly 1 selection in Multiple mode, the header icon behaves identically to Single mode (one icon crossfade).

**Layout support:** all 4 layouts adapt cleanly (confirmed at checkpoint — none needed a redesign): Stacked/Compact List/Split by Status add `renderProfilesSummaryRow`/`renderPlusFeatureRow` alongside their existing free/paid renderers; Card Grid's `grid-cols-3`/`grid-cols-2` CSS is unaffected since the capped lists never exceed their pre-existing row counts by much. Split by Status's right column renders the profiles-summary row then the capped feature rows in its existing single "With Plus" column — its 58%/42% split is unchanged.

**Stage 3 (upsell + Plus welcome) now also supports Multiple mode** — see "Multiple-mode stage 3" below for the full writeup; `effectiveJtbdKey` (`OnboardingV2`, Single mode's own `selectedJtbd` or Multiple mode's `selectedJtbds[0]`) is still what both screens receive as their primary `jtbdKey` (unchanged), but they now ALSO receive `selectionMode`/`selectedJtbds` directly, mirroring what `TunedResult` already gets.

**Analytics:** the task's "stage events include selection_mode" requirement has no infrastructure to attach to — no analytics system exists anywhere in this codebase (confirmed absent). Noted here as a gap rather than fabricated.

| Component | Path | Purpose |
|-----------|------|---------|
| `ProfilesSummaryRow` | `src/app/onboarding-v2/tuned-result/ProfilesSummaryRow.tsx` | Multiple mode only — the Plus section's profiles row (intro text + one tangible pill per interest); `unlocked` prop (stage 3 only) switches it to its active visual; `layout: "row" \| "stacked" \| "card"` matching each result layout's tile shape |
| `jtbdMerge.ts` | `src/app/onboarding-v2/lib/jtbdMerge.ts` | Merge engine (`mergeFreeSettings`, `mergePaidFeatures`, `buildProfilePreviews`, `rankFreeSettings`, `rankPaidFeatures`, `capList`, `outcomeForEnabled`, `outcomeForPaid`), `SETTING_CONFLICT_RESOLUTION`, `TuningResultLike` — the SINGLE shared engine reused by the result screen AND both stage-3 screens |
| `SETTINGS_RANK` / `FEATURES_RANK` | `src/app/onboarding-v2/lib/jtbdTuningResult.ts` | Editorial priority maps feeding the capped/ranked Multiple-mode lists above |

### Multiple-mode stage 3 (upsell + Plus welcome)

Ports the SAME merge/rank/cap engine used by the result screen into stage 3's two screens, so all three tell one continuous, consistent story for the same multi-selection: result = "here's your merged free tune + what Plus would add" (locked); upsell = "here's what VPN Plus turns on, based on your {count} picks" (top-ranked features, more of them); welcome = "here's your setup for {count} interests, now unlocked" (the SAME rows the result screen showed, paid now active). Both screens gain `selectionMode`/`selectedJtbds` props (defaulting to Single mode's exact pre-existing behavior) and the same `isMultipleActive = selectionMode === "multiple" && selectedJtbds.length >= 2` gate `TunedResult` uses — with exactly 1 selection, both screens render byte-identically to Single mode (no separate singular-count string was needed).

- **Upsell (`components/VPNPlusUpsell.tsx`):** Single mode's `JTBD_UPSELL[jtbdKey].benefits` (a hand-curated, per-JTBD copy source, unrelated to the tuning engine) is entirely untouched. Multiple mode instead builds `mergePaidFeatures(selectedJtbds)` → `rankPaidFeatures` → `capList(., UPSELL_MULTIPLE_HIGHLIGHT_CAP)` (`lib/jtbdUpsell.ts`, cap = **3** — deliberately independent from the result/welcome screens' own `paidFeatureCap` of 1, confirmed at checkpoint: the upsell is a persuasive screen that reveals more of the SAME ranked list, never a different one, so there's no disagreement about which feature ranks highest, only how much of that one list each screen shows), mapped into `BenefitCard`-compatible `UpsellBenefit` shape (`outcome`/`featureName`/`tooltip` straight from the merged feature; `learnMore: true`, unused by `BenefitCard`'s rendering). Subtitle: `upsellSubtitleMultiple(count)` → "Based on your {count} picks, here is what VPN Plus turns on." Pricing, hero image, CTAs, the streaming-logos block, and `UPSELL_EVERYTHING_ELSE` are all unchanged.
- **Plus welcome (`components/PlusWelcomeState.tsx`):** Multiple mode rebuilds the EXACT same `result`/`profiles`/`paidFeatures` `TunedResult` builds for this selection — `mergeFreeSettings`/`mergePaidFeatures` → `rankFreeSettings`/`rankPaidFeatures` → `capList(., freeRowCap)`/`capList(., paidFeatureCap)` → `buildProfilePreviews` — so the welcome shows the identical set of rows the result screen did, just with the paid item(s) now animating locked→unlocked. `totalRows` uses the same "capped free + 1 profiles row + capped Plus features" formula. Subtitle: `plusWelcomeSubtitleMultiple(count)` (`lib/jtbdUpsell.ts`) → "Your setup for {count} interests is now fully unlocked. Here's everything protecting you." (confirmed wording), replacing `JTBD_PLUS_WELCOME[jtbdKey].subheading` (Single mode, untouched). Confetti and the `unlockSettleDelay`/`unlockConfettiDelay`/`unlockChipFadeMs`/`unlockTransformStagger` timers are entirely unchanged.
  - **Extending the locked→unlocked transition to the Multiple-mode Plus feature row(s):** each layout's `renderPlusFeatureRow`/`renderPlusFeatureCard` (previously only had a static "locked vs. Plus-active" branch, no LIVE transition capability) gained the same `if (unlockTransition) { … }` branch its sibling single-mode `renderPaidRow`/`renderPaidCard` already had — `TransformingPaidCell` for Compact List/Split by Status/Card Grid, Stacked's own bespoke inline transform (`UnlockedChip` + badge↔check crossfade) for Stacked — reusing the exact existing choreography, just fed a `MergedPaidFeature` instead of a single JTBD's `PaidFeature`.
  - **Profiles row unlock:** `ProfilesSummaryRow` gained an `unlocked?: boolean` prop (default `false` — zero change to the result screen's own calls, which never pass it). When `true` (stage 3 only): the left glyph swaps from `CircleSlashIcon` to the same green checkmark asset every other unlocked row uses; each `ProfilePill`'s icon opacity and text color animate from the locked (dimmed/muted) to unlocked (full-opacity/white) treatment, via a simple CSS `transition-opacity`/`transition-colors` (not the more elaborate staggered `TransformingPaidCell` machinery, since profiles aren't part of that per-item stagger sequence — they unlock in sync with the overall `unlockTransition.unlocked` boolean instead). Each layout passes `unlocked={unlockTransition?.unlocked ?? paidUnlocked}` — so a Plus-plan user reaching the RESULT screen (stage 2) in Multiple mode now also correctly sees the profiles row as active, matching how Single mode's own paid rows already react to `paidUnlocked` there (previously `ProfilesSummaryRow` had no plan-awareness at all and always rendered the locked look).
  - Split by Status's right-column header now also tracks the live `unlockTransition.unlocked` value in Multiple mode (previously hardcoded to never flip in `profiles` mode), so it reads "Also active with Plus" in sync with the profiles/feature rows once they unlock.
- **Incidental fix:** `CardGridLayout.tsx`'s single-mode `renderPaidCard` called `toneOutcome(...)` without importing it (a pre-existing bug, unrelated to Multiple mode, that would throw at runtime for a Free-plan user viewing locked paid cards in the Card Grid layout) — fixed by adding the missing import while working on the adjacent `renderPlusFeatureCard` code.

**Consistency (verified):** for any given selection, the result screen's single displayed Plus feature is always the SAME feature that leads the upsell's top-3 list and the SAME feature the welcome unlocks — all three derive from one `mergePaidFeatures` → `rankPaidFeatures` call per the selected JTBDs, with only the display cap differing per screen (1 / 3 / 1). No screen can show a feature the ranked union doesn't agree is top-ranked.

## Integration

- **Sign In step (`components/SignInScreen.tsx`), inserted before Stage 1:** clicking "Start onboarding experience" no longer goes straight to Stage 1 — it now sets `AppState` to a new `"signin"` value, rendering a full-screen Sign In step. **Rendered inside the app window chrome, same convention as the rest of onboarding** (confirmed at checkpoint, not a raw full-viewport screen): the native Windows wallpaper backdrop + a centered bordered window box + `onboarding-v2/components/WindowChrome.tsx`'s titlebar (its "X" wired to `App.tsx`'s `handleCloseOnboarding`, same convention as `OnboardingV2`'s own `onClose` — returns to the start screen), so it reads as one continuous window throughout the whole flow rather than a separate screen.
  - **Every size/spacing/color value below is pulled directly from Figma** (file `VPN | Windows | Explorations`, `6DnFYq5H71PDiErYp5PtUD`, node 8909-184014 "V1" — fetched via the `user-figma` MCP once access was restored; the earlier `[UNVERIFIED]` best-effort version has been superseded): the window box is **1100×750** (not 1024×768 — this screen's own Figma frame, taking precedence over `OnboardingV2`'s size for this specific screen), border `rgba(255,255,255,0.2)` 1px, shadow `0px 2px 32px rgba(0,0,0,0.37), 0px 32px 64px rgba(0,0,0,0.37)`, `#16141c` fill, `8px` radius. Inside it, a centered **450px-wide column** (32px gap) holding: the logo lockup (mark + wordmark baked into one graphic, per the user-provided asset — `src/app/assets/proton-vpn-logo-signin.svg`, a real vector, not the earlier raster PNG which looked pixelated on HiDPI displays; exactly 45px tall — no separate `<span>` text label is rendered alongside it, per an earlier explicit request) + the **card** (450px wide, `32px` padding, `24px` gap, `8px` radius, `rgba(22,20,28,0.6)` fill, `rgba(255,255,255,0.04)` 1px border, `0px 0px 200px rgba(24,22,32,1)` shadow, `52px` backdrop blur) over a **layered ambient glow** (not from Figma — decorative, matching the app's `#6d4aff` purple + teal accents): two blurred radial orbs (purple centered behind the card, teal along the bottom) plus a softer radial wash on top, since the real design's ambient visual comes from a background image this reconstruction doesn't include. **Above the gradient, below the sign-in content:** a decorative ciphertext grid (`components/sign-in/SignInEncryptionGrid.tsx`, config in `components/sign-in/signInEncryptionConfig.ts`) — a canvas full-bleed monospace glyph texture (18px glyphs, ~8% peak opacity, bluish muted tone, center-weighted radial fade + CSS mask), with 6 random single-character crossfades every 400ms (320ms fade); flip candidates exclude the foreground card/logo rectangle and heavily masked edges, 75% of picks favor existing numeric/symbol cells, and regenerated glyphs have an 80% digit/symbol bias; freshly flipped glyphs brighten to ~6× opacity for ~2.2s then ease back; continuous canvas loop when motion is allowed; static (no flips) under `prefers-reduced-motion`; `pointer-events-none`, no new dependencies.
  - **Card contents:** heading block (`8px` gap) — "Sign in" title (`28px`/`36px`, weight 600, centered) + "Enter your Proton Account details." subtitle (`14px`/`20px`, weight 400, centered, `rgba(255,255,255,0.7)`); input fields block (`16px` gap, see `FigmaTextField.tsx` below); buttons block (`8px` gap) — primary "Sign in" (`#6d4aff` fill, `4px` radius, `10px` icon-gap, padding `5px 16px 7px` — asymmetric vertical padding, matching the input fields' own convention) + secondary "Create account" (`#4a4658` fill, `rgba(255,255,255,0.1)` 1px border, `4px` radius, `8px` gap, same padding) — both buttons' text is `14px`/`20px` weight 600 ("Body/Body Strong").
  - Both fields are real, editable, and clearable — prefilled with `"user@proton.me"` / a dummy `"Passw0rd1"` — with **no validation anywhere**: "Sign in" always proceeds regardless of field contents. Clicking "Sign in" disables the button and swaps its label to "Signing in…" + a spinner (reusing `onboarding-v2/components/Spinner.tsx`) for `SIGN_IN_TIMING.loadingDurationMs` (2000ms, config), then calls `onSignIn` — `App.tsx`'s `startOnboarding` (the exact same logic "Start onboarding experience" used to run directly: clears the `MakeYoursModal`/welcome-banner one-time flags, sets `currentStage` to `"connection"`, sets `AppState` to `"onboarding"`). "Create account" renders but is intentionally inert (`onClick={undefined}`) — no account-creation flow exists. Entrance is a plain opacity fade on the window box (~250ms; `prefers-reduced-motion` skips it, appearing instantly) — no new dependencies; copy centralized in `SIGN_IN_COPY`, timing in `SIGN_IN_TIMING`.

**The two input fields (`components/FigmaTextField.tsx`) ARE pulled from Figma** — the design system's "Input" component set (file `Vibing redesign`, `7eOURItUVknEQh0PBIRgqS`, node 165:9148), fetched via the `user-figma` MCP once access was restored, and matched pixel-for-pixel across every documented state/type:
  - **Default** (idle, empty): field background `rgba(255,255,255,0.05)`, no border, muted (`rgba(255,255,255,0.5)`) placeholder, solid-white (`#FFFFFF`) label — all text `Segoe UI Variable`, 14px/20px ("Body" style).
  - **Hover** (idle, unfocused): field background `rgba(255,255,255,0.1)` (plain Tailwind `hover:`, no JS state needed).
  - **Filled** (idle, has a value, unfocused): the design system uses the IDENTICAL `rgba(255,255,255,0.05)` fill as Default — only the text itself (white input value vs. muted placeholder, handled natively via `::placeholder`) distinguishes it, so no separate CSS state was needed.
  - **Focus (default or filled)**: field background `rgba(255,255,255,0.2)` + a 2px solid white BOTTOM-only border (not a full outline — reserved via an always-present, normally-transparent `border-b-2` so its appearance never shifts the field's box size) + a trailing icon appears (clear `×` for the email field, the eye toggle for the password field) — exactly matching the component set, where the Default/Hover/Filled/Error variants have no icon node at all and only the two Focus variants do.
  - **Error**: field background `rgba(247,96,123,0.05)` + a 2px solid `#F7607B` bottom border + a caption row below the field (16px warning-triangle glyph + message, `#F7607B`, `Segoe UI Variable` 12px/16px "Caption" style) — shown regardless of focus, since the component set defines no separate "focus + error" variant. (Not currently triggered anywhere — this screen has no validation — but the component supports it via an `error` prop for future use.)
  - Field padding is the exact Figma spec: `5px 12px 7px` (asymmetric vertical padding), `4px` border radius, `8px` gap between label and field.
  - Icons (clear ×, eye/eye-off, warning triangle) reuse `lucide-react` (`X`, `Eye`/`EyeOff`, `AlertTriangle` — already a project dependency) rather than the Figma file's own icon components (`ic-cross`/`ic-eye`/`ic-exclamation-triangle-filled`) — same "no new artwork" precedent followed throughout this app. The trailing icon button uses `onMouseDown` + `preventDefault` so clicking it never blurs the input first.
- `App.tsx` start screen shows a single primary **"Start onboarding experience"** button — the `PrototypeControls` HUD is present here too (`preStart` prop), showing **"Stage: 0 - Empty"** instead of a real numbered stage, so the Version/Layout/Tone of voice controls can pre-select stage 1's content before onboarding even begins (`currentStage` defaults to `"connection"`, so these controls already target the connection stage on this screen). The connection-stage content variant is chosen from the prototype-controls dropdown labeled **"Version"**; for the tuning AND upgrade stages, that same dropdown slot is relabeled **"Layout"** (`flatLabel` in `PrototypeControls`, derived from `usesResultLayout = stage === "tuning" || stage === "upgrade"`) and its options come from `STAGE_VERSIONS.tuning`/`STAGE_VERSIONS.upgrade` (both the same 4 `ResultLayout` values, not content versions) — this is one shared `resultLayout` state, not two independent dropdowns; changing it from either stage's HUD updates both. A separate **"Tone of voice"** `<select>` (shown for stages in `STAGE_SUPPORTS_TONE`, i.e. connection) sits alongside it. `App.tsx` renders `<OnboardingV2 onExit={...} variant={...} resultLayout={...} tone={...} onStageChange={...} />`; `onExit` runs the app-transition. `variant` only affects the first three states; `resultLayout` affects both the tuning stage's result-step arrangement AND the upgrade stage's Plus-welcome result (`PlusWelcomeState`) — see "Stage 3 reuses stage 2's layouts" above; the tone affects only the connection stage's copy (and, per "Tone of voice" above, stage 2 — `PlusWelcomeState` also now takes `tone` so its outcome sentences match what the user saw in stage 2 for the same JTBD).
- Flow control lives in `OnboardingV2`'s phase state machine. JTBD **Continue** advances straight from `jtbd` to `tuned` (there is no `tuning`/loader phase anymore) → **upsell**. JTBD **Skip** and both upsell CTAs (**Get VPN Plus** / **Continue free**) call `onExit`. The `jtbd`→`tuned` transition is an overlapping crossfade (~500ms). Both are **transparent overlays** over the persistent dimmed/blurred protected map (`dim` is true for `jtbd`/`tuned`/`upsell`), so the map's teal `StatusGradient` is the shared background and stays continuous through the crossfade. The upsell itself is an **opaque** split-screen (`#16141c` left, black right) that fully covers the map.

**`onExit` hands off the selected JTBDs to the main app:** `onExit?: (selectedJtbds?: JtbdId[]) => void` — Single mode passes its one pick as a 1-item array; Multiple mode passes the full ordered selection; Skip (abandoning onboarding without committing to any intent) passes `[]`. `App.tsx`'s `handleEnterApp` stores this in `onboardingJtbds` state and threads it through `ISPRegulationsPanel` → `CountryBrowser` (`src/app/components/CountryBrowser.tsx`, the main app's country/profile browser — outside the `onboarding-v2` tree, documented here since it's the direct consumer of this handoff): when non-empty, `CountryBrowser` defaults its nav to the **Profiles** tab (instead of Countries) and generates the Profiles list from these intents via `JTBD_TO_PROFILE_ID` — a fixed map from each JTBD to the one of the app's existing 6 hardcoded profile entries that best matches it for SERVER TARGETING/icon/P2P-tag purposes (`streaming`→"Streaming US" entry, `gaming`→"Gaming" entry, `downloading`→"P2P" entry, `bypass`→"Anti-censorship" entry, `privacy`→"Maximum security" entry, `travel`→"Work and school" entry, the last mirroring the tuning data's own "Home country profile"/business-glyph association for travel) — reordering/filtering the SAME 6 existing profiles, never inventing new ones or new artwork. Each mapped profile's displayed **title** is then overridden to `JTBD_PROFILE_LABEL[jtbd]` (`lib/jtbdData.ts` — the same short intent label the tuning stage's picker/profile-preview already use, e.g. "Travel", "Privacy", "Access") rather than the underlying entry's own generic title, so the name shown always matches what the user actually picked in onboarding — only the icon/subtitle/P2P-tag come from the matched entry. Falls back to the full default list (default titles) + "Countries" tab when there's no selection to draw from (onboarding skipped, or `skipOnboarding` bypassed onboarding entirely).

**Onboarding-profiles banner (`CountryBrowser.tsx`):** a slim inline note directly beneath the "Profiles ({count})" header, above the first profile row — NOT a floating toast. Shown only when `hasOnboardingIntents` is true (in this prototype, that boolean IS the "at least one profile came from onboarding" signal — every displayed profile is either entirely onboarding-derived or entirely the static default set, never a mix, since "New profile" has no real creation logic to seed a manual one; confirmed at checkpoint, no per-profile marker needed) AND the user hasn't dismissed it (`localStorage["profilesOnboardingBannerDismissed"]`, a dedicated key separate from every other one-time flag in the app — confirmed at checkpoint). Copy: `PROFILES_ONBOARDING_BANNER_COPY.message` = "We created these profiles from what you told us matters. Switch between them anytime." + the reused `checkmark-circle-filled.svg` glyph (confirmed at checkpoint — no new artwork) + a small `×` dismiss control that persists the flag permanently on click. Fades in (~200ms) when the Profiles list first shows; `prefers-reduced-motion` skips the fade (present immediately, same dismiss behavior). Never blocks selecting a profile or "New profile" below it.
- `TunedResult` currently receives `userPlan="free"` (no plan concept exists in the codebase yet); pass `"plus"` to render all 5 items as applied in every layout.
- **"Skip"** (prototype utility): a small top-right button, shown during all stage-1 phases across every connection variant (rendered once in `OnboardingV2` itself, above the per-variant overlay, so it isn't duplicated across v1/v2/v4/v4-split). Clicking it sets `phase` straight to `jtbd`, bypassing `unprotected`/`connecting`/`protected` entirely, and sets a `skippedConnection` flag that persists for the rest of the session. Since no VPN connection actually happened, `skippedConnection` overrides the usual "protected once you reach the tuning/upgrade stage" assumption — the persistent map keeps showing the user's real location with the unprotected/red pin and status gradient for the remainder of the flow, instead of flying to the VPN server in teal.

## Final personalization modal (`MakeYoursModal`)

Simplified to **two choices only**: theme and auto-start. Copy: title "Set it up your way", subtitle "A couple of choices, then you're all set.", theme heading "Pick your theme" (System/Dark/Light cards, no card subtext), auto-start row "Stay protected from the start" + "Launch Proton VPN and connect automatically every time you start your computer." + toggle (no leading icon — the row's `ShieldCheck` glyph was removed; label/description/toggle only), buttons "Apply"/"Not now". All copy centralized in the component's `COPY` object (i18n-ready, no framework exists in the repo).

**Removed from this modal (not from the app):** the NetShield row and the Default-country row + selector. Both features remain fully intact elsewhere — `netShieldEnabled` (`App.tsx`, default `true`) is still read by the right rail (`WorldMap`/`RightVpnFeatures`); `connectedCountry` (`App.tsx`, default `"Netherlands"`) is still set via the real connect flow (`WorldMap`'s `onConnect` → `handleConnect`). Neither value was ever *sourced from* this modal — it was just an additional editor — so removing their rows here changes nothing about their defaults or availability. One caveat worth noting: this modal's NetShield row *was* the only interactive toggle for `netShieldEnabled` anywhere in this prototype (no other Settings screen exists yet to flip it), so `setNetShieldEnabled`/`handleCommitNetShield` in `App.tsx` are now unused (the latter was removed as dead code; the state itself and its default are untouched, ready for a future Settings surface to wire up). Default country had no equivalent gap — the real "connect to a country" flow in the main app already existed independently of this modal.

**Behavior:** theme applies live on click (unchanged mechanics — `useTheme().setTheme`, not staged). Auto-start is staged (`stagedAutostart`, default **ON** — `AUTOSTART_DEFAULT`) and only written to `localStorage("autoStartEnabled")` on a button click; nothing else in the codebase currently reads that key. **"Apply"** commits whatever the user staged. **"Not now"** (and Escape/overlay-click, which map to the same handler) instead **reverts to `AUTOSTART_DEFAULT`, discarding any toggle change** — confirmed via checkpoint — so dismissing the modal can never silently leave auto-start off; the two buttons are only ever equivalent when the user leaves the toggle at its default. The modal still shows once (`SHOWN_KEY` in localStorage) and both buttons set it, unchanged from before.

**Icon:** the old Auto-start row's hatched placeholder icon (`assets/modal-placeholder-icon.svg`, also used by the removed Default-country row) was dropped; the row briefly used a plain `lucide-react` `ShieldCheck` glyph, then that too was removed — the row now shows no leading icon at all, just the label/description + toggle.

### Post-onboarding welcome banner (`components/WelcomeBanner.tsx`)

A calm, auto-dismissing acknowledgment of the onboarding→main-app handoff — the true final beat of onboarding, firing once right after `MakeYoursModal` closes (via either "Apply" or "Not now" — both funnel through the same `onClose` prop, now `App.tsx`'s `handleModalClose`).

- **Trigger:** `handleModalClose` sets `showWelcomeBanner` true (once — guarded by a dedicated `WELCOME_BANNER_SHOWN_KEY` = `"welcomeBannerShown"` localStorage flag, confirmed at checkpoint as a separate key from the modal's own `SHOWN_KEY` rather than reusing it, since they gate two different things even though both happen to get cleared together on `startOnboarding` today — the banner replays alongside the modal on every prototype demo run, not literally "once ever"). Threaded down `App.tsx` → `WorldMap` (`showWelcomeBanner` prop) → `WelcomeBanner` (`trigger` prop).
- **Placement (confirmed at checkpoint):** directly below the connection card/Disconnect button, not top overall (crowded) or bottom (the connection-details area there is only visible during the onboarding-entrance transition, not persistently empty as first assessed — being immediately under the card reads more clearly as "acknowledging the handoff you just completed"). `WorldMap.tsx` wraps it in a container sharing the connection card's exact horizontal bounds (`left: panelWidth + 32, right: "155px"`) so it's centered directly under the card/button, not centered on the whole map area (which is off-center due to the right-side feature rail) — `top-[150px]`, just below the card's own `top-[24px]` + rendered height.
- **Copy/timing:** `WELCOME_BANNER_COPY.message` = "You're all set — Proton VPN is protecting you." + a `lucide-react` `ShieldCheck` glyph (reused, no new artwork). `WELCOME_BANNER_TIMING`: entrance 250ms (slide+fade, y: -12→0), dwell 4000ms, exit 250ms (slide+fade out). `prefers-reduced-motion` drops the slide on both ends (fade only), same dwell.
- **Non-blocking by construction:** the positioning wrapper is `pointer-events-none`, so it never traps clicks on the connection card/Disconnect button underneath or anywhere else; there's no dismiss control at all — it purely times out via its own internal `dwellMs` timer and unmounts via `AnimatePresence`'s `exit`.
- **No existing toast/banner component to reuse** (confirmed at checkpoint): `components/ui/sonner.tsx` is dead shadcn/`sonner` scaffold, never mounted anywhere and tied to `next-themes` (not set up in this codebase, separate from the app's own `ThemeContext`) — building a small, self-contained component was confirmed as the right call rather than wiring that up.
- **Tone:** deliberately calm — no confetti/sound (that's the Plus-welcome screen's own moment, during onboarding itself, elsewhere), single sentence, no CTA/tips/upsell.

## Unprotected entrance animation ("surveillance lock-on")

The Unprotected screen plays a cinematic ~4.5s entrance on mount. The screen starts as the dark map only (everything else hidden) and reveals element by element while the map zooms toward the user. All timing lives in `lib/entranceTiming.ts` (`ENTRANCE_TIMING`).

Timeline (T=0 = mount):

| T (ms) | Element | Motion |
|--------|---------|--------|
| 0 | Map | Starts at world view (zoom 2), `flyTo` user coords (zoom 5) over 3500ms (Leaflet) |
| 500–2000 | Status gradient | Fades 0→1 (Framer) — danger tint creeps in |
| 1500 | Pin | Springs in (scale 0→1, overshoot) via CSS `ob2-pin-in` |
| 2000 | Pin pulsation | Concentric rings begin (CSS animation-delay) — not before |
| 2000 | Reticle | Animated targeting reticle scales/fades in (overshoot), then its layers animate continuously |
| 2500 | Padlock | Fade + drop from above |
| 2800 | Headline | Fade + slide up |
| 3100 | Subtext | Fade + slide up |
| 3200 | Info card | Rises from below |
| 3400 | Card rows | Stagger in 150ms apart (explicit per-row delays) |
| 4000 | CTA | Rises + scale; perpetual breathing glow (CSS `btnGlow`) |

Implementation notes:

- The map's `flyTo` starts at T=0 to the seeded fallback coords; when live IP detection resolves with different coords, a second `flyTo` (1.6s) redirects. The map never waits for the API.
- Pin entrance + pulsation gating are pure CSS (no setTimeout): the spring uses `animation-delay` = `pinAppear`; the pulse ring uses `animation-delay` = `bracketsStart`.
- `OnboardingMapV2` skips the first reactive run of its fly/status effects (`flyInit`/`statusInit` refs) so the mount entrance is not overridden.
- Info card shows skeleton bars (`loading` = unprotected && `!isLive`) and crossfades to real values when live data arrives. CTA is disabled (opacity 50%) until `isLive`.
- Per-phase delays are conditional: only `phase === "unprotected"` uses the long entrance delays; later phases use a quick stagger so transitions stay snappy.

## Phase-transition Choreography

- **Unprotected → Connecting** (button click): button scales/fades out; gradient coral→slate; padlock→spinner; reticle fades out (250ms); scramble starts +400ms; map zoom-out begins.
- **Connecting → Protected** (VPN confirmed, min 3.2s): spinner→lock; gradient slate→teal; `flyTo` server; teal pin; card rows stagger in; Continue fades in after.
- **Protected → JTBD** (Continue): centered overlay fades out; map dims; left column slides in from -40px, right from +40px.

## Stacking / z-index (important)

Leaflet assigns its internal panes z-index values of 200–700 (tilePane 200 … markerPane 600 … popupPane 700). Any overlay rendered as a sibling of the map must sit above this range or it will be painted over by the tiles (symptom: content flashes once, then the map covers it). v2 handles this by:

- isolating the Leaflet container with `isolation: isolate` so its pane z-indices stay contained, and
- rendering the centered overlay and the JTBD panel at `z-[1000]`, the title bar at `z-[1200]` (matching v1's convention).

## Prototype: Flow overview

A secondary **"View flow overview"** button sits below the start screen's primary "Start onboarding experience" (`App.tsx`, ghost/`border-white/20` style, matching `VPNPlusUpsell`'s "Continue free" convention — the primary button's own style/behavior is untouched). Both buttons are stacked vertically, same fixed width (`w-[320px]`) and same font size (`text-[17px]`/`leading-[22px]`), 16px apart, inside one shared glassmorphic container — `rgba(0,0,0,0.4)` + `backdrop-blur-md` (translucent, blurring the wallpaper behind it), rounded (`16px`), `24px` padding. Clicking the secondary button sets `AppState` to `"overview"`, rendering `FlowOverview` (`src/app/components/FlowOverview.tsx`) full-screen in place of the start screen; its top-left "Back" (same chevron+label affordance as `TunedResult`'s Back button) and Escape both return to `"start"`.

**Purpose:** a read-only diagram for a reviewer to understand the whole flow at a glance — what each stage is for and how it works. Titled **"Onboarding flow overview"**. It renders the 4 `ONBOARDING_STAGES` (`STAGE_ORDER` order) as equal-size cards in a horizontal row with connector chevrons between them (32px, `flex-col` → `lg:flex-row` responsive breakpoint stacks them vertically with downward connectors on narrow windows); a staggered fade+slide entrance (`staggerChildren: 0.07`, ~250ms per card, connectors fade in the same stagger sequence) plays once on mount — `prefers-reduced-motion` (via the shared `useReducedMotion` hook) drops to a plain fade with no stagger, nothing loops.

**Hover focus effect:** hovering any card scales it up (`whileHover={{ scale: 1.1 }}`) and dims every OTHER card to `opacity: 0.4`, so a presenter can visually isolate the card they're discussing. Driven by one `hoveredStage` state (not per-card `whileHover`, since dimming siblings needs to know what's hovered elsewhere); each card is split into an outer wrapper (owns the entrance stagger/opacity via `cardVariants`, untouched) and an inner wrapper (owns the hover scale + sibling-dim animation, `z-20` while hovered so the scaled-up card renders above its neighbors) so the two animations never fight over the same properties. Disabled entirely under `prefers-reduced-motion` (no scale, no dimming).

Each card shows, top to bottom: a real, representative **screenshot** of that stage (a full-bleed image spanning the card's own width via a negative-margin trick against the card's `p-[24px]` padding, clipped to the card's rounded corners by its `overflow-hidden`, `aspect-[4/3]` + `object-cover object-top`), then a 48×48px icon tile (`ShieldCheck`/`SlidersHorizontal`/`Settings2` from the already-installed `lucide-react` at 26px, or the real `vpn-plus-badge.svg` asset for "Upgrade to Plus" — no new artwork) sitting ABOVE the (`text-[19px]`) stage number + name line, then one fixed **Goal** sentence, and one fixed **How** sentence (both `text-[15px]`/`leading-[20px]`). The 4 screenshots (`src/app/assets/flow-overview/stage-{connection,tuning,upgrade,personalization}.png`, one representative screen from each stage's actual flow — connection's exposure screen, the JTBD picker, the VPN Plus upsell, and the "Set it up your way" modal) are looked up per stage via `STAGE_SCREENSHOT: Record<OnboardingStage, string>`. On wide windows (`lg:` and up) the row container has no upper width cap (`max-w-[1800px]`) and each card is `flex-1` (grows to fill the available row width, replacing an earlier fixed `w-[240px]`) — on narrow/stacked layouts each card instead caps at `max-w-[420px]` so it doesn't stretch edge-to-edge. The screen no longer shows a subtitle under the title, nor the live variation chips ("Version"/"Layout"/"Tone") or the "Testing: N x · M y" line that an earlier iteration had — removed per explicit request; `FlowOverview` now takes no props besides `onBack` and doesn't read the prototype's variation state (`variant`/`resultLayout`/`tone`) at all. All copy (title/labels/per-stage Goal+How sentences) is centralized in `FlowOverview.tsx`'s own `COPY` object, matching the i18n-ready convention every other prototype screen here uses (no framework exists in the repo). Cards are informational only — clicking one does nothing; there's no way to jump into a stage from this screen.

## Prototype: PRD overview

A **"View PRD details"** entry sits below the start screen's primary/secondary buttons, inside the same glass container — same ghost/`border-white/20` box style, width, and font size as "View flow overview" (identical styling, per explicit request — no longer a plain text link). Clicking it sets `AppState` to `"prd"`, rendering `PrdOverview` (`src/app/components/PrdOverview.tsx`) full-screen; Back (top-left, same convention) and Escape both return to `"start"`. It's `FlowOverview`'s sibling: same navigation pattern, same dark canvas (`bg-[#0a0a0f]`), same card tokens (`rounded-[12px]`/`border-white/8`/`bg-white/3`), same one-time `staggerChildren` entrance (`prefers-reduced-motion` → plain fade, no stagger, nothing loops).

**Purpose:** a stakeholder-glance artifact — absorb the onboarding PRD's key figures/principles/risks in under a minute of scanning. Every figure is VERBATIM from the PRD (never rounded, recomputed, or invented) and centralized in `PrdOverview.tsx`'s own `COPY` object. It's a single scrollable page, each section a distinct visual pattern (never consecutive prose blocks):

1. **Hero** — headline + subline only (the "Onboarding PRD" overline and the scope chips row — "Windows"/"Android"/"MVP" + the muted "Out: …" chip — were removed per explicit request)
2. **The problem** — 4 one-sentence statement cards, 2-col grid
3. **The numbers** — 6 stat tiles (big numeral + small muted caption)
4. **Success targets** — 4 full-width "now → target" rows (NOT small chips — a string like "Android free D1/1W/1M: 37/48/27% → 42/53/32% (+5pp)" can't fit a chip at any reasonable width, so this renders as wrapping rows instead — a layout choice, not a copy edit) + 2 stat tiles (`≥50%`/`≥98%`) + 1 footnote line (the muted guardrail card — "Paid up-front conversion must not deteriorate" — was removed per explicit request)
5. **Six principles** — numbered 2×3 grid (number + title + one line each)
6. **Top risks** — 4 "risk → mitigation" rows, one line each

**Removed per explicit request** (originally part of this screen, no longer present): the "The flow" section (the step-strip diagram, its "steps 2–6" caption, and its cross-link to `FlowOverview` — so `PrdOverview` no longer takes an `onViewFlowOverview` prop, just `onBack`), the figures footnote ("Figures from the onboarding PRD — activity retention cohorts, signups Jan–May 2026."), and the repeated "← Back" link at the bottom of the page (top-left Back + Escape remain the only way back).

No i18n framework or analytics infrastructure exists anywhere in this repo (confirmed repeatedly) — copy lives in `COPY` per the established convention; no analytics calls were fabricated for "on open/back," consistent with every other feature here.

## Notes / Unverified

- `[UNVERIFIED]` In-browser visual QA was not run automatically (no browser tool available in this environment); validate at `http://localhost:5174` (v2 button). The production build compiles cleanly.
